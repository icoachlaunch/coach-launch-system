# PROGRESS LOG — Coach Launch System Build

**READ THIS FIRST in any new chat.** It's the single source of truth for where the build is up to,
what the standards are, and what to build next. Update it whenever a piece is finished.

> ## 🔒 STANDING RULE — UPDATE THIS FILE EVERY TIME (non-negotiable)
> This is a required step, not an optional one. PROGRESS.md must be updated:
> 1. **At the START of every new chat** — read it first to catch up (that's how we resume).
> 2. **The MOMENT anything changes** — a piece is finished, a decision is locked, a name changes,
>    a structure is re-scoped, or a file is added/edited. Don't wait until the end of the session.
> 3. **Before ending any session** — do a final pass so the next chat opens with everything in order.
>
> **What to update, every time:** (a) the `Last updated:` header line — date + one-line summary of
> what changed; (b) the relevant Status entry (✅ done / ⬜ next); (c) any new decision, canon change,
> or coined name; (d) the "NEXT" pointer so it's always clear what to build next.
>
> If you (Matthew) ever notice this file is stale, say "update PROGRESS" and I'll bring it current
> before we do anything else.

> ## 🔒 STANDING RULE — ADD EVERY NEW PROMPT + GUIDE TO THE DASHBOARD (non-negotiable, per Matthew 2026-07-10)
> Every time a new builder prompt + companion guide is created (or an existing one is renamed/rebuilt),
> it MUST be surfaced in `dashboard.html` (the client Training Portal) in the SAME session — not "later."
> This means, every time:
> 1. **Guide card:** add/point the module's Guide buttons at `guides/<name>-guide.html` (Open ↗ + Download).
> 2. **Prompt buttons:** add a `data-copy="<name>-prompt"` + `data-dl="<name>-prompt" data-name="<name>-prompt.md"` button pair.
> 3. **Embed the prompt:** add the FULL prompt text to the `<script id="prompt-data" type="application/json">` block under the key `<name>-prompt`, so Copy/Download work hosted OR offline (no fetch/CORS dependency).
> 4. **Flip any placeholder:** if the item had a greyed "coming soon / in build" card, replace it with a **live** module card (copy the template from the most recent live card; use `<span class="pill live">Live</span>`).
> 5. **Verify:** JSON still parses; every guide link resolves on disk; every `data-copy`/`data-dl` key exists in the JSON.
> How (safe method that beats the truncation gremlin): edit `dashboard.html` with an **in-VM Python script** —
> parse the JSON block, add the key from the prompt `.md` file, string-replace the placeholder card with the
> live-card template, write back to the mount. Don't hand-edit the ~470KB minified HTML.
> Mirror the same addition into `index.html` (Matthew's internal hub) too.

> ## 🔒 STANDING RULE — EVERY FORMULA-STEP GUIDE CARRIES THE 3-PILLAR / 9-STEP MAP (non-negotiable, per Matthew 2026-07-11)
> Every companion guide for a **Formula step (1–9)** MUST show the 3-Pillar / 9-Step map directly under the
> hero `<header>`, with a crimson **"You are here"** marker on the guide's current step. This is the journey
> locator — it tells the reader where they are in the system. Applies to all future steps (4–9) as they're built.
> 1. **The map's CSS lives ONCE in `assets/coach-launch-guide.css`** (`.map`, `.map-grid`, `.step`, `.step.here`,
>    `.here-tag`, etc.). Do NOT re-inline it per guide.
> 2. **The map's HTML is generated by `scripts/apply_formula_map.py`** — the single source of truth for the block.
>    Run it on the guide; it inserts the map after `</header>` (or replaces an existing one — idempotent) and
>    strips stray trailing NUL bytes:
>    `python3 scripts/apply_formula_map.py guides/<name>-guide.html [--step N] [--asset "Display Name"]`
>    Step is inferred from the filename for all known guides (incl. Steps 4–9: sniper-close, cash-flow-engine,
>    genie-x-converter, goliath-content, pixie-dust-social, dragon-fire-ads); pass `--step` for anything new.
> 3. **Sub-part guides** (e.g. the 3 $Million Story™ parts, the 4 Red Diamond Offer™ parts) mark their PARENT
>    step and the intro reads "built inside Step N, the <Step Name>"; a step's own guide reads "is Step N".
> 4. **Brand Foundations guides (Brand Profile · Visual Style · Brand Voice) do NOT get the map** — they sit
>    before the Formula's first pillar (Matthew's call, 2026-07-11).
> 5. **Verify:** exactly one `class="map"` per step guide, map after the hero, correct step flagged, 0 NUL bytes.

Last updated: 2026-07-15 (✅ **STEP-4 BLOCK RENAMED → THE SNIPER PRESENTATION™** (per Matthew, this chat: "this block needs to be called the Sniper Presentation™; the Sniper Close™ will be a separate block built in another chat"). Files renamed → `prompts/sniper-presentation-prompt.md` + `guides/sniper-presentation-guide.html`. Updated display name in the prompt (tool = "the Sniper Presentation™ Builder"), guide (hero/title/eyebrow/CTA/footer), dashboard.html (Step-4 card title → "Sniper Presentation™"; `#prompt-data` key → `sniper-presentation-prompt`, **embedded==disk verified**), index.html (Step-4 section + cards), and `scripts/apply_formula_map.py` (`sniper-presentation`→Step 4 + asset). **"The Sniper Close™" kept ONLY as the closing SEQUENCE = a SEPARATE future block** (its own chat; `sniper-close-*` slug now free for it). **Canonical Step-4 name in the 3-pillar map stays "The Sniper Close™"** (persona canon, unchanged). The dashboard's **"Sniper Presentation™ Slides" card = The Presentation Engine™** (the separate 07-15 build) — left untouched. Host-verified (Grep + Desktop Commander, because the bash `/sessions` mount served STALE reads mid-edit — falsely showed 14 keys/NUL/no card): dashboard JSON parses, 14 keys (…+`sniper-presentation-prompt`+`sniper-presentation-slides-skill`), no stale `sniper-close-*` refs, index 20 links resolve. — prior same day: ✅ **DASHBOARD BUTTONS tidied to ONE action each** (`dashboard.html`, per Matthew): every module card now shows a single **Open** button for the Companion Guide and a single **Download** button for the Builder Prompt. Removed the guide's redundant Download link (15) and the prompt's **Copy** button (14, incl. the "Copy skill" variant on the Presentation Engine™ slides card), and de-ghosted the survivors so both remaining buttons share one consistent primary style (`cl-btn ghost` 29→0). Structure intact (29 `act-btns` groups preserved), ™ unchanged (1943 before==after), 0 mojibake. Also fixed "How to use" step 02 ("Copy the prompt…" → "Download the prompt, then paste it…") so the instructions match the new buttons. **dashboard.html ONLY** (Matthew: leave `index.html` this time). Done on the real Windows file via Desktop Commander + .NET `UTF8Encoding($false)` (the safe method — Get-Content/Compress-Archive corrupts non-ASCII). *"Highlighted as a step" styling deferred — Matthew sending a separate instruction. Data-copy JS handler now unused but harmless; data-dl download (embedded `#prompt-data` JSON) still powers the Download button offline.* **⚠️ the sandbox `/sessions` mount served a STALE PROGRESS.md this session (showed 07-11 state) — verified/edited against the real host file; trust Desktop Commander + Read/Grep, not the mount.**) — prior: 2026-07-14 (✅ **HERO-HEADER STANDARD CHANGED — BADGE DROPPED (all guides congruent)**. Matthew flagged the hero repeated "The $100K Day Formula™" too many times (big title + badge pill + map line = 3x) and required the hero to be **congruent across ALL guides**. **DECISION (Matthew, via AskUserQuestion): drop the bottom `.cl-badge` pill from every guide's hero** — "The $100K Day Formula™" now shows ONCE as the `formula-title` and once on the map line below. **APPLIED:** removed the `<span class="cl-badge">The $100K Day Formula™</span>` line from **all 14 guides** (in-VM Python; verified badge=0, `</html>` intact, 0 NULs on every file). The template (`templates/guide-template.html`) already had no badge (uses `.cl-meta-row` chips) → already congruent. **NEW HERO STANDARD (locked):** logo → `formula-title` "The $100K Day Formula™" → `guide-name` → pillar-led `cl-eyebrow` → `subtitle` → (NO badge) → the map. Also fixed the Presentation Engine™ eyebrow to lead with the pillar like the rest: "Money Magnet™ · Production Tool · Pairs with Step 4". **NOTE for future guides:** do NOT add the `.cl-badge` pill to the hero. **The `.cl-badge` CSS stays in the stylesheet (still used as a component elsewhere), just not in the hero.**) — prior same day: (✅ **THE PRESENTATION ENGINE™ — BUILT** (new **production tool**, not a Formula step — turns a finished asset, above all the **Sniper Presentation™ Script** from Step 4, into an on-brand HTML slide deck the client presents from their own laptop). **THREE ARTIFACTS + WIRING:** (1) **`slide-viewer.html`** — a rebuilt, improved single-file offline deck player (Coach Launch crimson chrome, Montserrat/Lato). **Improvements over the reference viewer:** a real **Presenter View** — press **P** to pop out a second-screen window (big speaker script + next-slide preview + timer + phase/act, hidden from the room; uses `window.opener` direct-DOM sync so it works offline on `file://`), plus **S** = in-window speaker-notes panel (rehearsal / single-screen), **T/R** = start-pause/reset **timer**, and a **run-sheet drawer grouped by phase**. SLIDES entries now carry `{type,label,phase,act,notes,html}` where `notes` = the SPEAKER SCRIPT. JS verified with `node --check`, 0 NULs, balanced backticks. (2) **`prompts/presentation-engine-prompt.md`** — full A–J (10/10 verified), one-step-per-turn, Slide-Architect/Adapter voice (coach speaks Matthew's voice; SLIDES carry the CLIENT's brand). **KEY DESIGN — the SLIDE vs SPEAKER SCRIPT split:** the Sniper Presentation™ Script already gives every beat a `SLIDE:` (→ clean audience slide) + `SPEAKER SCRIPT:` (→ presenter notes) + PHASE/ACT (→ run-sheet) — so the first-class path is a near 1:1 beat→slide conversion, NOT design-from-scratch. Contains: the **Sniper-beat→slide-type map**, a **SCORE™/Sniper slide-type library** + Coach Launch-token skeleton, **distinct story-bit slides**, **AUTO-BRAND** (pull tokens from the Business Brand Profile + Visual Style Guide), **reusable saved-slide templates**, images via the existing **`kie-image-gen` skill** (NOT a hand-rolled script), and TERMS NEVER TO USE. Validation = pass/fail **Quality Check, NO /25** (assembles from already-locked upstream — same precedent as SCORE™ Card / Enrollment Doc / Event Magnet™ / Cash Flow Max™ / Sniper Presentation™). Legacy-clean (grep — legacy only in the TERMS block); 118 ™; 0 NULs. (3) **`guides/presentation-engine-guide.html`** — locked depth+visual standard (12 H2 sections: what-it-is, the kit, **the presenter-view upgrade**, setup [Cowork/web/one-off], build-your-first-deck, how-each-beat-becomes-a-slide [+ Sniper map table], auto-brand, images, reusable templates, tips, common mistakes [Do/Don't], where-it-fits + Is-It-Ready check); green Remember quote + generic CTA card (Tool = **The Presentation Engine™**). **CARRIES THE STANDARD FORMULA MAP** (Matthew's correction 2026-07-14: EVERY training-guide header must be identical) — stamped via `scripts/apply_formula_map.py --step 4 --asset "The Presentation Engine™" --intro "…"`, flagging **Step 4 · The Sniper Close™** as "You are here" (the step this production tool serves) with a custom intro noting it isn't itself a Formula step; registered `presentation-engine`→Step 4 in STEP_BY_FILE + ASSET_BY_FILE. Verified: 1 map, header-before-map, 0 NULs. 62 ™. **DECISIONS LOCKED w/ Matthew (via AskUserQuestion):** name = **The Presentation Engine™** (de-legacied from the reference's "AI Presentation Engine"); scope = **any deck, but a first-class Sniper Presentation™ path**; **all 4 improvements** (presenter view + run-sheet/timer · auto-brand · SCORE™/Sniper slide-type library + story-bit slides · reusable templates); packaging = house builder-prompt + guide + dashboard/index wiring. **REFERENCE:** modeled on Fletcher's "AI Presentation Engine" (guide + Slide Builder SKILL.md + slide-viewer.html, all supplied by Matthew) — studied for STRUCTURE ONLY, rebuilt original in Coach Launch canon, legacy stripped. **WIRED:** dashboard.html — new **live "Production Tool" module card** inserted right after the Step 4 Sniper Close™ card in the Money Magnet™ (#money) section (Guide Open/Download + Prompt Copy/Download), prompt embedded as the **14th** `#prompt-data` key (verified: embedded==disk, 14 keys, all data-copy/data-dl map, 14/14 guide links resolve, ™ 1815→1935, 0 NULs). index.html — new "Client Deliverables · Production Tool · The Presentation Engine™" section (guide + prompt cards; 22/22 links resolve) + Growing note updated. Backups `dashboard.html.bak-preengine` + `index.html.bak-preengine` on disk. **NEXT still: Step 5 · Cash Flow Engine™** (unchanged — the Presentation Engine™ was a side-build at Matthew's request; the thank-you/registration video is still flagged for Step 5).) — prior: 2026-07-14 (✅ **STEP 4 · THE SNIPER CLOSE™ — BUILT** (builder prompt + companion guide + dashboard/index wired) + **SPEAKER BIT LIBRARY SEEDED**. **BUILD SUMMARY:** `prompts/sniper-close-prompt.md` (927 lines; full A–J verified 10/10; one-step-per-turn; gated on SCORE™ Card + Money Model + Red Diamond Offer™ + $Million Story™ + Magic Formula™; branches on FORMAT [Masterclass / 3-day Challenge] + CLOSE [Enrollment / Strategy free-or-paid / Deposit]; VIP Hour Growth+ start-or-end; integrates the bits/ library with a story-bit slot in every act; 239 ™; legacy-clean). `guides/sniper-close-guide.html` (locked depth standard: 13 H2 sections incl. what-it-is/isn't, where-it-fits, the SCORE™-spine table, the two formats, the three closes, a deep-dive per phase, the story-bit layer, the VIP Hour, an illustrative worked before→after, hard rules, 6 common mistakes, and an "Is It Ready?" **pass/fail 8-point Ready Check — NO /25**; green Remember quote + generic CTA card [Tool = the Sniper Close™ Builder]; Formula map stamped **Step 4** via `scripts/apply_formula_map.py --step 4 --intro "…"` → 1 map, header-before-map, 0 NULs; 74 ™). **WIRED:** dashboard.html Step 4 placeholder → **live** module card (Guide Open/Download + Prompt Copy/Download) and the prompt embedded as the **13th** key in `#prompt-data` (verified: embedded == disk, 13 keys, all data-copy/data-dl mapped, 13/13 guide links resolve, ™ 1575→1815, 0 NULs); index.html new "Step 4 · The Sniper Close™" section (guide + prompt cards; 20/20 links resolve) + Growing note updated → next Step 5. `apply_formula_map.py` already had `sniper-close`→Step 4 in STEP_BY_FILE. Backups `dashboard.html.bak-presniper` + `index.html.bak-presniper` left on disk (mount `rm` permission-blocked, as before). **VOICE/CANON:** built in Matthew White's voice; the KEY framing — the Sniper Presentation™ is the SCORE™ Card delivered LIVE (arranges locked upstream, doesn't re-create). **NEXT: Step 5 · Cash Flow Engine™** (Money Magnet™) — incl. the thank-you / registration-page video asset flagged this session. **DESIGN DETAIL (locked earlier this session):** **STEP 4 NAMING/SCOPE (locked w/ Matthew):** Money Magnet™ stays the Pillar-2 name (unchanged). Step 4's tool = **the Sniper Close Builder**, which outputs a **Sniper Presentation™** (the full live pitch) that culminates in the **Sniper Close™** (the closing sequence). **ONE single flow — NOT split into parts.** **THE SPINE:** Matthew's own one-page card ("Sniper Close Builder") is the model. Its 5 acts ARE the **SCORE™** arc: Act1 Serendipity=S · Act2 Connection=C · Act3 Opportunity=O · Act4 **Remove Risk**=R (his name for Reality) · Act5 Execution=E. Grouped into his 4 card phases w/ named transitions: **Phase 1 WHO (Open Loop)** · **Phase 2 WHAT (Token)** · **Phase 3 WHY (Sniper Close)** · **Phase 4 WHEN (Re-Pitch)**. **TWO FLEX AXES:** (1) **Format** — event names are ONLY **Masterclass** (single day, short→long by phase) and **Challenge** (multi-day = **3-day**); the 4 Accelerator phases (Launch/Execute/Grow/Mastery) are unchanged and set how deep/long it runs; Challenge = the same spine split across 3 days (Day1=Phase1+belief, Day2=Phase2 system, Day3=Phases3–4 close). (2) **Close** — the canon **Three Closes**: **Enrollment · Strategy (free or paid) · Deposit** (Matthew is updating the card's old Zero-Pitch/$299/$997 labels to these). **VIP HOUR:** gated **Growth+**, placement is a CHOICE (pre-event OR end-of-event when live attendance is thin). **THANK-YOU / LANDING-PAGE VIDEO** (the video on the post-registration thank-you page): decided it's a **separate asset that belongs with Step 5 · Cash Flow Engine™** (VIP-upsell variant = cash-in at registration = the Cash Flow Engine™ mechanic; plain thank-you+show-up-hype variant when no VIP) — NOT part of Step 4, and NOT the Genie X Converter™ (Step 6 = back-end follow-up messaging, a different system). Flagged for Step 5. **KEY INSIGHT:** the Sniper Presentation™ is the *live-delivery* format of the **SCORE™ Card** (Step 3 Part 2) — same relationship as Fletcher's Workshop(live)/VSL(recorded) both run off SCRIPT. So it ARRANGES the locked upstream (SCORE™ Card + Money Model + Red Diamond Offer™), doesn't re-create it. **REFERENCES STUDIED (structure only, legacy stripped — same call as prior parts):** Fletcher `winning-workshop-builder-prompt.txt` + `script-vsl-builder-prompt.txt` (uploads), winning-workshop-guide + script-vsl-guide (Fletcher vercel site), AND Matthew's OWN assets: the Sniper Close Builder card + the **INVISIBLE PITCH™/BLUEPRINT** masterclass worksheet (Google Doc). Blueprint→SCORE map: VIP Hour→pre-event; Part 1 Intro & System Origin Story (=**$Million Moment™**)→S+C; Part 2 Overarching System (=**Magic Formula™** 3 core steps, Hell→Heaven, Two Identities)→O; Part 3 Internal Objection→Remove Risk/belief-bust; Part 4 External Objection→Re-Pitch. Fletcher pitch beats to fold into Phases 3–4: Broken Models→Process→Proof→Value Stack→Anchor→Bridge→Price→Fork in the Road→Urgency→Guarantee→CTA cadence→Q&A. **⭐ NEW CONCEPT — SPEAKER BITS + THE BIT LIBRARY (Matthew's, this session):** a **speaker bit = a timeless, repeatable STORY** slotted into the presentation to do ONE job and raise emotion → more sales. Jobs: **engagement · hype · empowering-belief · belief-break · objection-break · close-prime.** Two placements (= folder names Matthew supplied): **Pre-Pitch** (front, Phases 1–2) and **Re-Pitch** (close, Phases 3–4 → fills the card's Belief/Bust-It + Objection/Handle-It rows). **NO Pitch-bits folder — the pitch IS the Sniper Close sequence.** Extracted a **6-part bit anatomy** from Matthew's examples (Monkey Paw, Law of Advancement, Giraffes vs Turtles, Information Alone, Two Biggest Expenses, Fear of Failure, Transformational Learning): **1 HOOK (engagement setup) · 2 STORY (metaphor w/ call-&-response) · 3 TURN (bridge to them) · 4 TARGET (belief/objection it breaks) · 5 SOUNDBYTE · 6 TRANSITION.** **DECISIONS (Matthew, via AskUserQuestion):** sourcing = **original + public-domain** (safe to distribute; classics retold in our words + credited; personal stories must be the client's OWN/true — golden rule); form = **resource folder to start** (not a tool). **BUILT `bits/`** in the project: `README.md` (how it works + anatomy + taxonomy + attribution rule + SCORE-slot map), `_bit-template.md` (generic skeleton), `pre-pitch/` (4: train-the-room, take-the-shot, the-bamboo, the-crossroads) + `re-pitch/` (6: get-in-the-room, release-the-grip, down-before-up, the-crab-bucket, the-real-price, the-time-bank) = **10 seed bits**, each verified complete (6/6 anatomy sections + tags). Facts verified via web: **Jordan** line = 1990 Nike "Failure" ad (written by Jamie Barrett) — quoted accurately; **monkey trap** = Pirsig's "South Indian monkey trap" (Zen and the Art…, 1974) — retold + credited; **86,400-second time bank** = widely-circulated parable — retold. Deliberately AVOIDED the disputed "learning-pyramid" 10%/70% retention stats (no fabricated numbers). **NEXT: build `prompts/sniper-close-prompt.md` (full A–J, one-step-per-turn, gated on SCORE™ Card + Money Model + Red Diamond Offer™, pulls bits from `bits/`) + `guides/sniper-close-guide.html` (locked depth+visual, map stamped Step 4), then wire dashboard.html + index.html, and remember the thank-you video for Step 5.** — prior: 2026-07-12 (✅ **Step 3 · Red Diamond Offer™ · Part 5 · CASH FLOW MAX™ — DONE** (builder prompt + guide). **NEW CANON: the Red Diamond Offer™ is now 5 PARTS (was 4)** — Cash Flow Max™ added as Part 5, which now completes the Red Diamond Offer™ AND the whole Offer Matrix™ (Pillar 1). **What it is:** the tool that builds the PAID cash-flow assets inside the funnel — the **VIP upsell** + the **order bumps** — that add real value to the masterclass and raise cart value. It's the fuel for the Cash Flow Engine™ (Step 5): cash-in at registration self-liquidates ad spend; the main offer is still closed at the event (Sniper Close™). **Distinct from the Event Magnet™:** Event Magnet™ = FREE, finds the hot step / masterclass theme / hook, pulls people IN; Cash Flow Max™ = PAID, fills the CART. **Built from scratch** (Matthew had no external reference; grounded in his own live VIP pages — coachlaunch.com/vip-upgrade-1a = single VIP w/ $47 + $9 order bumps; supernovaoffer.com/soc-diamond-1 = double VIP). **DECISIONS LOCKED w/ Matthew (2026-07-12):** (1) placement = Red Diamond Offer™ **Part 5**, inside the **Offer Matrix™** (offers get BUILT in Pillar 1, MONETIZED by the Cash Flow Engine™ in Pillar 2 — same pattern as the Event Magnet™); (2) name = **Cash Flow Max™** (™ every mention; VIP / order bump / bump / cart value / value stack / value anchor stay PLAIN); (3) TWO internal parts: **Part 1 · VIP Upsell Strategy**, **Part 2 · Order Bumps**; (4) **phase-driven WITH a strategic override** — follow the phase by default (Launch none · Execute single VIP ~$47 · Growth single VIP ~$97 + 1 bump · Mastery 2-option VIP + 2 bumps), override only with a reason; (5) **dual-mode: BUILD from scratch OR AUDIT + upgrade existing** assets; (6) VIP **core-2 components LOCKED** = Recordings/Replays + VIP Q&A/extra live time; standard stacker = **Community / inner-circle access** (rapport); optional stackers (day-after session, workbook, fast-start training) only if real; (7) single vs double VIP (double = 'everything in A, plus…' premium option, best at Mastery); (8) bumps **$7–$47** (never below $7), 4 formats (templates/swipe · cheat sheets/checklists · mini-trainings/recordings · tool kits/calculators); (9) **every asset pulled from the Magic Formula™** + gets **its own $Million Promise™**; (10) **buyer-first / honest-anchor** rules (real value not junk; defensible value figures). **Validation = qualitative 7-point Ready Check, NO /25** (On-System · Phase-Right · Own Promise · Real Value · Easy Both Ways · Honestly Anchored · Cart Adds Up) — same no-score precedent as the $Million Moment™ / SCORE™ Card / Enrollment Doc / Event Magnet™ (it assembles from already-scored/locked upstream). Files: `prompts/cash-flow-max-prompt.md` + `guides/cash-flow-max-guide.html`. Verified: A–J complete (10/10), legacy-clean (grep), ™ correct (30 in prompt, 22 in guide), clean END markers; guide map stamped via `scripts/apply_formula_map.py --step 3 --asset "Cash Flow Max™"` (1 map, Step 3 flagged 'You are here', header-before-map, 0 NULs). Also **registered `cash-flow-max` / `cashflow-max` → Step 3** in the map script's STEP_BY_FILE + ASSET_BY_FILE so future re-runs auto-infer. **dashboard.html + index.html both updated:** Red Diamond Offer™ → '5 parts'; Part 5 live module card added (Guide Open/Download + Prompt Copy/Download); Cash Flow Max™ prompt embedded as the **12th** key in the `#prompt-data` JSON (in-VM Python edit — JSON parses, 12 keys, all data-copy/data-dl map to keys, all 12 dashboard guide links resolve, ™ preserved 1362→1583, 0 NULs); index.html = 'all five live' + Part 5 guide/prompt cards + growing note (18/18 links resolve). **ALIGNED TO THE CASH FLOW ENGINE™ CALCULATOR (Matthew, same day):** https://icoachlaunch.github.io/cash-flow-calculator/ — Cash Flow Max™ BUILDS the assets, the calculator MODELS the numbers. Added a **"Know Your Numbers"** section to the guide + a **THE NUMBERS** block to the prompt (Section F) + Step-7 "model the numbers" + deployment note, teaching: **pre-close revenue** (ticket + bump + VIP cash-in) self-liquidates ad spend; the calculator's **take-rate targets** (order bump **30–40%** · VIP 1 **15–25%** · VIP 2 **10–20%** · paid thank-you upgrade **15–25%**); **show rate** baseline **15–25%** → **~60%** with a free bribe (the biggest lever); what each pricing/take decision does to the return; and **VALIDATE-BEFORE-YOU-PROGRESS** (each phase is a checkpoint — prove show/take/close numbers before moving up). These are Matthew's OWN benchmarks from his OWN tool, so citing them is on-canon (not fabricated). Re-embedded the updated Cash Flow Max™ prompt into dashboard.html and re-synced event-magnet from disk — verified all **12/12 embedded prompts == disk**, JSON parses, 0 NULs, all data-copy/data-dl map. Re-verified via host Grep (bash mount was stale): prompt A–J 10/10 + 4 calc refs; guide 1 map + Know-Your-Numbers + calc link + legacy-clean + `</html>`. *(Standing lesson recurred: the bash `/sessions` mount served STALE reads — final state verified via the Read/Grep tools on the host FS. `dashboard.html.bak` left on disk; the mount `rm` was permission-blocked.)* **NEXT: Step 4 · The Sniper Close™** (start of Pillar 2 · Money Magnet™). — prior: 2026-07-11 (✅ **AGENTS SECTION headline + subhead updated** (`dashboard.html`): headline → "Meet Your Perfect AI Agent Team"; subhead → "The ultimate AI support workforce of managing and closing more clients than ever before." Also built a reusable **`kie-image-gen` skill** (Kie.ai generate → in-chat preview/approve OR N variations to pick from → download via the Desktop Commander local MCP; packaged as `kie-image-gen.skill`, source in `skills/kie-image-gen/`, repackage with `skills/pack_skill.ps1`). **⚠️ ENCODING + BIG-FILE LESSON:** edit the ~500KB minified `dashboard.html` on the REAL Windows file via **Desktop Commander** using .NET `[IO.File]::ReadAllText/WriteAllText` with `UTF8Encoding($false)` — do NOT use PowerShell `Get-Content -Raw` / `Compress-Archive` (PS 5.1 mis-decodes no-BOM UTF-8 and double-encodes every ™/—/·). First pass corrupted all non-ASCII (™ dropped 1362→1); caught it with a ™-count check and restored from `.bak`. Redo via .NET UTF-8 = clean (™=1362, 0 mojibake). Also: the sandbox `/sessions/...` mount was serving badly STALE reads this session — trust Desktop Commander (real FS) for verification, not the mount. — **AGENT PHOTOS GENERATED (photoreal headshots, Kie.ai).** Matthew supplied his Kie.ai API key. Key finding: `api.kie.ai` IS reachable from the Cowork sandbox now (the old 403 proxy-block is gone) — BUT `google/imagen4` returns instant 500s ("Internal Error"), so the working model is **`google/nano-banana`** (Gemini 2.5 Flash Image, ~4 credits/img, 1:1). Generated all six matched studio headshots (light-gray warm-pink-tint bg): Maya/Leo/Sofia/Marcus/Priya/Jack. **Still can't DOWNLOAD from the sandbox** — every image CDN (`tempfile.aiquickdraw.com` etc.) + `web_fetch` are blocked, and there's no api.kie.ai download proxy — so the JPGs must be fetched on Matthew's machine. Delivered TWO PowerShell scripts: **`scripts/download_agent_photos.ps1`** (no key; downloads the six exact URLs I just generated straight into `assets/agents/` — run SOON, tempfile links are temporary) and updated **`scripts/generate_agent_photos.ps1`** (swapped broken imagen4 → nano-banana, embedded the six matched prompts, removed unsupported negative_prompt/seed fields — durable regenerate path, prompts for key). Showed Matthew a live in-chat preview gallery of all six. **Once he runs the download script, the dashboard `.agent-photo` imgs pick them up automatically (SVG fallback until then). `assets/agents/` still empty on disk (README only).** — prior same day: **DASHBOARD (`dashboard.html`) restructured into 3 CLEAR PILLAR PARTS + a clickable Formula map.** (1) Replaced the old `.roadmap` 3-pillar chips with the guide's **Formula map design** (dark pillar bars, crimson step badges, taglines) — now a `.map dash-map` where **every step is a link that jumps to that module's section** (1→#story, 2→#formula, 3→#offer, 4–6→#money, 7–9→#clients). (2) Added **big pillar banner headers** delineating **Pillar #1 · Offer Matrix™**, **Pillar #2 · Money Magnet™**, **Pillar #3 · Client Flywheel™** (`.pillar-banner`, dark band + crimson kicker), so the portal reads as 3 clear parts: Foundations (start here) → Pillar 1 (Steps 1–3, all live) → Pillar 2 (Steps 4–6) → Pillar 3 (Steps 7–9). (3) Split the single `#upcoming` "Coming Next" section into `#money` (Steps 4–6) and `#clients` (Steps 7–9) under their banners. (4) Top nav now points at the 3 pillar anchors. Edited via in-VM Python (file is ~500KB minified — no hand-edit). Verified: 9/9 anchors resolve, 0 dangling hrefs, 9 map step-links target correct sections, `#prompt-data` JSON still parses (11 keys), old roadmap HTML gone, 0 NUL bytes. **Note:** `index.html` (internal hub) NOT yet mirrored — do when convenient. **NEXT: Step 4 · The Sniper Close™.** — prior same day: **FORMULA MAP is now a standing rule across every Formula-step guide.** Added the 3-Pillar/9-Step map (with a crimson "You are here" marker) to all 8 step guides that were missing it — Step 1 parts (million-story, million-promise, golden-avatar, million-moment) flag Step 1; Step 3 parts (money-model, score-card, enrollment-doc, event-magnet) flag Step 3; magic-formula already had it (Step 2). Moved the map's CSS out of magic-formula's inline `<style>` into the shared `assets/coach-launch-guide.css` so it's inherited once. Built `scripts/apply_formula_map.py` — the idempotent generator + single source of truth for the map HTML (infers step from filename incl. Steps 4–9, strips stray NUL bytes); normalized all 9 step guides through it. Also cleaned trailing NUL padding out of magic-formula-guide.html and million-story-guide.html. Per Matthew, the 3 Brand Foundations guides are intentionally left WITHOUT the map. **NEXT: Step 4 · The Sniper Close™** (when built, run the script on its guide — the map is handled automatically). — prior: 2026-07-10 (✅ **Step 3 · Red Diamond Offer™ · Part 4 · THE EVENT MAGNET™ — DONE** (builder prompt + guide). This **completes the Red Diamond Offer™ (all 4 parts) and the entire Offer Matrix™ / Pillar 1.** The Event Magnet™ = the free, visual lead magnet seeded by the **hot step already flagged in the Magic Formula™** (Step 2), that fills the Money Magnet™ events (Pillar 2). Modeled on Matthew's supplied legacy "lead magnet" builder + guide — studied for STRUCTURE ONLY, rebuilt original in Coach Launch canon (same call as the Money Model / SCORE™ Card / Enrollment Doc; legacy names live ONLY in the TERMS NEVER TO USE block). **Validation = qualitative pass/fail (the 6-Point Event Magnet™ Check), NO /25** — per Matthew's "follow what the example recommends" (the example uses a 5-criteria pass/fail gate, not a score) and because the hot step + promise it's built on were already scored/locked. **Stripped the reference's cost-per-lead stats** ($1.80–$3 / $10+) — those are the legacy brand's, not ours; only $24M/$5.2M cited. **Did NOT coin an "Authority Amplifier" equivalent** — the pairing video is taught unbranded, and the nurture is pointed to the canon Genie X Converter™ (Step 6). Files: `prompts/event-magnet-prompt.md` + `guides/event-magnet-guide.html`. Verified: A–J complete (10/10), legacy-clean (grep), ™ correct, clean end markers. `index.html` AND `dashboard.html` both updated (Part 4 cards live; "all 4 parts / Offer Matrix™ complete"; dashboard now embeds **11** prompts and the Event Magnet™ is a live module card — verified 11/11 guide links resolve, 11/11 prompt buttons map to embedded JSON). **NEXT: Step 4 · The Sniper Close™** (start of Pillar 2 · Money Magnet™). — prior: Added **BRAND FOUNDATIONS** — 3 new client-facing tools, each builder prompt + guide: **Business Brand Profile · Visual Style Guide · Brand Voice** (built before the Formula). Rewired `dashboard.html`: new "Brand Foundations · start here" section ahead of Step 1, template + internal-reference cards retired from the dashboard. See the Brand Foundations Status entry below. — prior: Added `dashboard.html` — the client/student **Training Portal**: every guide + builder prompt organized in build sequence, with per-item Open/Download guide + Copy/Download prompt actions; all 7 prompts embedded so copy+download work hosted OR offline. See its Status entry below. — prior: Part 3 · THE RED DIAMOND OFFER™ ENROLLMENT DOC — **REBUILT to V2** after Matthew supplied his Red Diamond Offer Generator + the 7X Red Diamonds framework. Now a LONG-FORM SALES LETTER on the 10-part Red Diamond Offer structure, flowing in the **SCORE™ 5-act arc** (S→C→O→R→E — the psychological flow behind ALL Coach Launch copy). Centerpiece = the **7X Red Diamonds** (the 7 value elements) + the **3 Levels** (ascension tiers). Value Stack renamed from "Offer Mechanics" (which stays = cash-in+close in the Money Model). Kept the DR blocks (Fork · Confidence Filter · Objection Crusher · P.S.) + added Fast Acting Bonus + FAQ. Validation = completion gate + qualitative Ready Check (no /25). Both files verified (A–J complete; legacy-clean; ™ correct). Next: Part 4 · the Event Magnet™.))

> **⚠️ NEW CANON (2026-07-09) — THE 7X RED DIAMONDS + THE 3 LEVELS (Matthew's high-ticket offer framework, from the "Red Diamond Offer Generator" one-pager).**
> **The 7X Red Diamonds** = the 7 elements that make a high-ticket offer desirable (stack only the REAL ones): **Opportunity** (unique access — a high-value person, a podcast, a rolodex) · **Proximity** (closeness to you + a peer group who've done it) · **Exclusivity** (selective access — mastermind, VIP group) · **Expert Access** (direct access to you, your team, brought-in experts/partners) · **Turn-Key** (done-for-you/done-with-you systems + templates) · **Experience** (events, community, retreats, getaways, VIP dinner) · **Accountability** (structure that makes them implement faster). Plain names, NO ™.
> **The 3 Levels** = the offer's ascension tiers (rising price; show all 3 so the reader sees the peak): **Level 1 · Confidence** (trainings + system + basic coaching) · **Level 2 · Acceleration** (turn-key / done-with-you, faster results) · **Level 3 · Proximity** (direct access to you — top tier). Plain names, NO ™. **"Proximity" is intentionally BOTH a Red Diamond AND the name of Level 3** — proximity to you is the core value of the top tier (by design, not a conflict).
> **The Red Diamond Offer one-page structure (10 parts):** Offer Name ($Million Promise™) · Key Problems (3) · Discovery Story ($Million Moment™) · Testimonials · Secret Ingredients (Magic Formula™) · 7X Red Diamonds (+ 3 Levels) · Fast Acting Bonus · Value Stack (the one-pager labels this "Offer Mechanics" — but in canon "Offer Mechanics" = cash-in+close in the Money Model, so on the offer doc it's the **Value Stack**) · Risk Reversal · FAQ. *(Note: on Matthew's graphic the Testimonials box is mislabeled "The Three Core Problems…" — a copy/paste typo to fix in the graphic.)*

---

## How to resume in a new chat

1. Start the new chat **inside the "Coach Launch AI Builder Prompts" project** (so the persona + brand rules load).
2. **Reconnect this folder** if it isn't already: `C:\Users\Focus Account\Documents\Coach Launch Builder Prompts`.
3. Say: **"Read PROGRESS.md and let's continue."** I'll catch up from this file and the existing work.
4. When we finish a piece, I update this file before we end.

One chat per piece is the recommended way to work — keeps each build focused.

---

## The System (canon — never change)

- Brand: **Coach Launch** · Founder voice: **Matthew White** ($24M high-ticket sales · $5.2M recurring client results — the only two figures).
- Vehicle: **The $100K Day Formula™** — 3 pillars, 9 steps: Offer Matrix ($Million Story → Magic Formula → Red Diamond Offer) · Money Magnet (Sniper Close → Cash Flow Engine → Genie X Converter) · Client Flywheel (Goliath Content → Pixie Dust Social → Dragon Fire Ads).
- Map: **The $100K Day Accelerator™** — 4 phases, 112 days: Launch → Execute → Growth → Mastery.
- **$Million Story (Step 1)** is built in **3 parts**, each with its own builder prompt + guide:
  1. **$Million Promise™** (the message) — ✅ DONE
  2. **The Golden Avatar™** (deep-dive buyer profile[s]) — ✅ DONE
  3. **$Million Moment™** (system-discovery story — formerly working-titled "System Origin Story") — ✅ DONE (builder prompt + guide)
- Golden rule: never invent frameworks/steps/prices/stats; ask instead. Never use legacy/other-brand terms (Fletcher, MDM, Customer Engine, etc.).

> **⚠️ CANON UPDATE (2026-07-08) — PILLAR RENAME.** The 3 pillars were renamed by Matthew: **Offer Engine™ → Offer Matrix™**, **Money Engine™ → Money Magnet™**, **Client Engine™ → Client Flywheel™**. All repo files updated. **ACTION FOR MATTHEW:** the project's **custom instructions (persona system prompt) still say the old names** — I can't edit those. Update them there so new chats load the right canon. Note: the step name **Cash Flow Engine™** (Step 5) is unchanged — only the pillars renamed.
>
> **⚠️ NEW COINED NAME (2026-07-08) — Event Magnet™.** The hot-step lead-magnet tool/asset is named **Event Magnet™** (distinct from the **Money Magnet™** *pillar*, to avoid collision). Chain: the **hot step** (flagged in the Magic Formula™) → seeds the **Event Magnet™** (the free lead magnet, its own tool, built downstream) → which fills the **Money Magnet™** events (Pillar 2). Event Magnet™ carries ™ on every mention. The Event Magnet™ builder tool itself is not built yet.
>
> **⚠️ NEW COINED NAME (2026-07-08) — Coach Launch Academy™.** The members' area where the builder tools + AI-guided coaching + premium 1-on-1 consulting live. **Relationship (Matthew):** the Academy is **inside** the **$100K Day Accelerator™** — you purchase the Accelerator and that unlocks the Academy. So in the guides' CTA: the **button sells the Accelerator** ("Join the $100K Day Accelerator™") and the **paragraph points to the tools in the Academy** ("Coach Launch Academy™"). Not a mismatch — two different things. Enrollment URL: **https://fast.coachlaunch.com**. Coach Launch Academy™ carries ™.

---

## Locked standards (do not drift)

- **Visual:** every doc uses `assets/coach-launch-guide.css` (light theme, crimson `#db0063` accent, Montserrat + Lato, real logo at top). See `style-guide/visual-style-guide.html`.
- **Hero header (hard standard, every guide — locked 2026-07-14, congruent across ALL guides):** logo (`.cl-brand-logo`) → `formula-title` "The $100K Day Formula™" → `guide-name` (last word in `<span class="hl">`) → pillar-led `cl-eyebrow` ("[Pillar]™ · Step N · [context]") → `subtitle`. **NO `.cl-badge` pill in the hero** (Matthew dropped it to stop "The $100K Day Formula™" repeating 3×; it now appears once in the title + once on the map line). Formula-step guides then carry the standard map (via `apply_formula_map.py`).
- **Guide voice:** Teaching Voice — plain, direct, 5th-grade, zero jargon. See `style-guide/teaching-voice-guide.html`.
- **Paragraphs (hard standard, all docs — guides, prompts, emails, posts):** 1–2 sentences per paragraph, then white space. No walls of text. Mostly one sentence; pair two only if closely linked. Split anything 3+ sentences. Encoded in `teaching-voice-guide.html` and the `p` spacing in `assets/coach-launch-guide.css`.
- **Trademark ™ (hard standard, every occurrence):** Every coined system name carries ™ on **every** mention. That's: The $100K Day Formula™, The $100K Day Accelerator™, the 3 pillars (Offer Matrix™ · Money Magnet™ · Client Flywheel™), the 9 steps ($Million Story™ · Magic Formula™ · Red Diamond Offer™ · The Sniper Close™ · Cash Flow Engine™ · Genie X Converter™ · Goliath Content™ · Pixie Dust Social™ · Dragon Fire Ads™), and the 3 Story parts ($Million Promise™ · The Golden Avatar™ · $Million Moment™). **NOT** the generic phase words (Launch/Execute/Growth/Mastery) and **NOT** the company name "Coach Launch". Both builder prompts instruct the AI to output ™ on every mention too.
- **Brand voice (selling/social):** `style-guide/voice-guide.html`.
- **Guide DEPTH standard = `guides/million-promise-guide.html`.** Every guide follows this model: rich "what it is" intro; where-it-fits; "how you'll build it" overview; a FULL deep-dive section per framework element (what it is / why it matters / how to find yours / good vs bad / traps); putting-it-together with filters; a copy-polish pass; a full worked before→after transformation; hard rules with reasons; Common Mistakes as full paragraphs (what it looks like / why it happens / why it costs / the fix); scoring bands; next step. **No summaries. Explain everything.**
- **Builder prompt standard = `prompts/million-promise-prompt.md`.** Full A–J architecture, Gatekeeper mode, one-step-per-turn, pre-written content in full, thresholds + /25 scoring, hard stop.
- **Naming:** guides `*-guide.html`, prompts `*-prompt.md`.
- **Formula map (hard standard, every Step-1–9 guide):** the 3-Pillar/9-Step map with a "You are here" marker sits directly under the hero. CSS is in the shared `assets/coach-launch-guide.css`; HTML is stamped by `scripts/apply_formula_map.py` (idempotent, filename-inferred step). Brand Foundations guides are exempt. See the STANDING RULE at the top.
- **Guide closing standard (2026-07-08):** every guide ends with (1) a green **`.cl-quote`** "Remember" motivational sign-off (green text on light-green, ONE combined paragraph — the single-sentence-per-paragraph rule is waived for these), unique per guide; then (2) the **`.cl-cta`** next-step card (replaces the old crimson `.cl-next` box). CTA is GENERIC/identical on every guide: H2 "Ready to Build Your $100K Day Masterclass" · H3 "The ultimate one-to-many client acquisition system" · P "Use the [Tool Name] tool in Coach Launch Academy™ to build yours with AI-guided tools and premium one-on-one consulting & coaching." (only [Tool Name] changes per guide) · crimson button "Join the $100K Day Accelerator™" → https://fast.coachlaunch.com (target _blank). Both components are in the shared `assets/coach-launch-guide.css`.
- **Internal vs client (important):** `style-guide/` (Visual Style, Brand Voice, Teaching Voice) and `templates/` are **INTERNAL reference only** — used to produce on-brand work, **never handed to clients**. The **only client deliverables are the builder prompts (`prompts/`) and companion guides (`guides/`)**. `index.html` is Matthew's internal hub and labels the two groups accordingly.

---

## Status

### ✅ Done
- Style system + assets: `assets/coach-launch-guide.css`, logo SVGs.
- Core style guide: `style-guide/visual-style-guide.html`, `voice-guide.html` (Brand Voice), `teaching-voice-guide.html`.
- Template: `templates/guide-template.html`. Library home: `index.html`. `README.md`, `SETUP-GITHUB.md`.
- **$Million Promise (Part 1 of $Million Story):**
  - Builder prompt: `prompts/million-promise-prompt.md` (titled "The $Million Promise Builder").
  - Guide: `guides/million-promise-guide.html` — **this is the locked guide-depth standard.**
- **The Golden Avatar (Part 2 of $Million Story):**
  - Builder prompt: `prompts/golden-avatar-prompt.md` (titled "The Golden Avatar Builder"). Full A–J, Gatekeeper mode, one-step-per-turn, gates on the locked $Million Promise. **Flow:** intake → evidence check → **Step 0: market-size & how-many-avatars** (aim ~5M+ people / ~1M+ businesses; overlap concentric avatars to reach a viable market; 1 avatar if bullseye already big enough; cap 3) → name all avatars → 7-field loop per avatar (answer per-avatar or batched).
  - **Scoring GATE (enforced in builder):** each avatar scored /25 across 5 fields; **locks only at 18/25+ AND a 3+ on every field.** UNVERIFIED (no-evidence) fields capped at 3. No avatar advances / no moving to Part 3 until every planned avatar is LOCKED.
  - Guide: `guides/golden-avatar-guide.html` — built to the locked guide-depth standard. De-emphasizes "3"; adds a market-sizing "how many avatars" section; scoring section rebuilt as a **color-coded numeric grid** (0–5 rubric with score badges, the 5 scored fields, and pass bands: 18–25 lock / 13–17 almost / 0–12 rebuild).
  - The 7 fields: Snapshot & Label · Situation & Demographics · Pains & Failed Attempts · Desires & the One Outcome · Objections & Beliefs · Watering Holes · Voice of Customer.
  - *Note:* "The Golden Avatar" is now the canonical name for Part 2 (replaces earlier "3 Avatar Profiles"). Cross-references updated in the $Million Promise prompt + guide and `index.html`. Old `avatar-profiles-*` files removed.

### ✅ DONE — Part 3: The $Million Moment™ (formerly "System Origin Story")
- **Name LOCKED:** **$Million Moment™** — sibling to the $Million Promise™, connects to the $Million Story™ family. The centerpiece is the pivotal discovery ("the moment") the whole story is built around.
- **CONCEPT LOCKED:** this is a **SYSTEM-DISCOVERY story** — how the coach discovered the method they now use. It lands on **"…and that's how I discovered the [Name] System."** The client names their own system (working name) during the interview; that **[Name] System is the seed of their Magic Formula™** (Step 2), where it gets formalized. So we DON'T force the name "Magic Formula™" into the story — the client's own system name carries it.
- **Builder prompt: ✅ DONE (V2)** — `prompts/million-moment-prompt.md` (titled "The $Million Moment™ Builder"). Full A–J. Gates on the locked $Million Promise™ **and** Golden Avatar™.
  - **Structure = Matthew's P.I.V.O.T. framework** (his own, supplied 2026-07): **P**ain Point · **I**dentity Crisis · **V**ital Moment (= the $Million Moment™) · **O**vercoming Process (system discovered + **named** here) · **T**ransformation Evidence — wrapped with **Rapport** openers and **Refinement** closers. P.I.V.O.T. written plainly, **no ™** (like FOCUS in the voice guide).
  - **It's an INTERVIEW, not a form.** One question per turn, **progressive disclosure** (easy → deep), **"what/how" not "why,"** sensory + body language, contrast (before/after) questioning, pull specifics over generalities, capture BOTH external events AND internal identity shift. Uses Matthew's actual interview questions.
  - **VOICE = Story Coach** (warm, safe, patient) — shifted OFF the hard "gatekeeper." Explicitly **heads off impostor syndrome** ("you have what it takes"). Still never invents the story FOR them.
  - **It's about the AUDIENCE, not the coach** — every stage ties back to the Golden Avatar™ mirror (their pain, identity fear, limiting belief, desired outcome).
  - **Anti-fabrication:** never invents the user's story, memories, mentors, numbers, results, or system name; borrowed/invented = fails. Matthew's $24M/$5.2M are HIS only.
  - **NO SCORING (by design — Matthew's call).** No /25, no points, no numeric lock gate. Passes a qualitative pass/fail **READY CHECK** (8 checks): true & theirs · pain-led & mirrored · identity shift present · Vital Moment specific · system named & born · real proof (not bragging) · emotion + facts balanced · plain & human, about them. All eight YES to finish. Mirror flagged UNVERIFIED if no Golden Avatar™.
  - **Cross-refs updated:** "System Origin Story™" → "$Million Moment™" in both existing prompts, both existing guides, and `index.html` (Part 3 builder card added). Old name kept only as a one-line "formerly" note.
  - *History:* V1 built it on the generic brand Story Arc with a /25 score — both replaced in V2 per Matthew (no scoring; PIVOT system-discovery interview).
- **Companion guide: ✅ DONE** — `guides/million-moment-guide.html`, built to the locked guide-depth standard (`guides/million-promise-guide.html`). Covers: what it is / is NOT (mirror, not a resume/highlight-reel/brag) · where it fits · how you'll build it · a dedicated **"Why It's an Interview (The Psychology)"** section (progressive disclosure · what/how not why · sensory/body · contrast · specifics · about-the-audience · impostor-syndrome callout) · **P.I.V.O.T. in full** (Rapport + P/I/V/O/T + Refinement, each: what it is / why it matters / how to find yours / ✓vs✕) · naming the [Name] System · putting it together · a **full worked before→after** (resume bio → a complete PIVOT discovery story ending "…that's how I discovered the 20-Minute Dad Method") · hard rules · 8 common mistakes w/ fixes · **"Is It Ready?"** section that explains WHY it isn't scored and gives the 8-item pass/fail Ready Check + green-light signal · next → Magic Formula™. ™ verified, PIVOT un-™, no legacy terms.
- `index.html` updated: Part 3 guide + builder cards both live; "all three parts live"; next = Step 2.

### ✅ Step 1 · The $Million Story™ is now COMPLETE (all 3 parts: $Million Promise™ + Golden Avatar™ + $Million Moment™).

### ✅ DONE — Step 2: Magic Formula™ (Offer Matrix™)
- **Builder prompt: ✅ DONE** — `prompts/magic-formula-prompt.md` (titled "The Magic Formula™ Builder — V1"). Full A–J, Gatekeeper mode, one-step-per-turn.
  - **Modeled on** a proven signature-system/roadmap builder architecture (external example Matthew supplied), rebuilt end-to-end in Coach Launch canon. All legacy terms stripped (Fletcher/MDM/Product Roadmap/Model Builder/O.P.T.I.N/etc. appear ONLY in the TERMS NEVER TO USE block).
  - **Concept:** Magic Formula™ = the client's branded **3-stage / 9-step signature system** — the "how" that delivers the $Million Promise™. Formalizes the **[Name] System** seeded in the $Million Moment™.
  - **Decisions locked with Matthew (2026-07-08):** (1) adopt the **3×3** structure as Step-2 canon; (2) keep all **3 business archetypes** (Product Creator / Journey Professional / Hybrid) driving outcome language; (3) the **hot step** is the single hottest ingredient of the Magic Formula™ — flagged as the **seed of the Event Magnet™** (the lead magnet; distinct from the Money Magnet™ *pillar*), NOT itself called "a money magnet" (kept, reframed, unbranded criteria checklist, no O.P.T.I.N); (4) the "8 perfect-offer characteristics" carried as **unbranded Quality Filters** (no Fletcher IP).
  - **Flow:** confirm $Million Promise™ (18+/25 gate) → confirm archetype → name the system (seed = $Million Moment™ [Name] System) + subtitle → capture/research method → build 3×3 outline (one stage at a time) → catchy step names → step-level outcomes (archetype-aware, /5 block on feelings) → **hot step** pick (seed of Event Magnet™) → Final Report.
  - **Scoring:** /25 across 5 elements (3×3 Structure · Stage Names · Step Names · Step Outcomes · Promise/Delivery). Bands: 20–25 green / 18–19 quick fix / <18 rebuild. Timeline-match rule enforced.
  - **Next → Red Diamond Offer™ (Step 3).**
- **Companion guide: ✅ DONE** — `guides/magic-formula-guide.html`, built to the locked guide-depth standard and the locked visual system (`assets/coach-launch-guide.css`). Covers: what it is / is NOT · where it fits (Offer Matrix™ Step 2; upstream $Million Story™; downstream Red Diamond Offer™) · the 3×3 (stage-grid visual + timeline-match check) · the 3 business types · the 8 unbranded Quality Filters · system naming (+ subtitle) · stage naming (taxonomy) · step naming · outcomes by type (5/4/3/2 scoring) · **the hot step → Event Magnet™** (explicit "don't confuse Event Magnet™ asset vs Money Magnet™ pillar" callout) · **two full worked examples** (Product Creator baby-sleep coach + Journey Professional immigration attorney — flagged illustrative, no fabricated results) · /25 rubric + bands · 6 common mistakes w/ fixes · Is-It-Ready checklist · next = Red Diamond Offer™. ™ verified on every coined name incl. Event Magnet™; no legacy terms.

  - **Top-of-guide MAP added (2026-07-08):** a self-contained CSS mockup of the full formula (3 pillars / 9 steps) now sits directly under the hero — styled to Matthew's reference infographics (dark pillar bars, crimson step-number badges, yellow taglines), with **Magic Formula™ = Step 2 flagged "You are here."** Uses the canon ™ pillar names (Offer Matrix™ / Money Magnet™ / Client Flywheel™); Matthew may prefer the flavored names from his graphics (Magic/Elite Offer Matrix · Money Magnet Events · Enchanted Flywheel). Step taglines were pulled from Matthew's own graphics. No external images — pure CSS, restyleable.

### ✅ DONE (2026-07-08 session) — TRUNCATION REPAIRS
A save-truncation bug had cut the tail bytes off several "done" deliverables. All now repaired and verified complete on disk (host filesystem — note the bash `/sessions/...` mount read-caches and lagged, so verify with the Read tool, not bash `tail`):
- **`guides/million-promise-guide.html` — REBUILT from scratch** to the locked depth standard (was truncated mid Part 1 at ~10KB). Full: 5 parts each in full (what it is / why / how to find yours / ✓vs✕ / trap) · Putting It Together + 4 filters · Copy-Chief Polish (3 versions) · full worked before→after (business-coach example, flagged illustrative) · Hard Rules · /25 scoring rubric + bands · 6 Common Mistakes w/ fixes · Is-It-Ready checklist · next → Golden Avatar™. ™ verified, no legacy terms.
- **`guides/golden-avatar-guide.html`** — tail rebuilt (Is-It-Ready checklist completed: 7 fields + lock gate + concentric + 18/25 no-field-under-3; next → $Million Moment™; footer; close).
- **`guides/million-moment-guide.html`** — tail rebuilt (Ready Check completed to all 8 items + green-light callout; next → Magic Formula™; footer; close).
- **`prompts/million-promise-prompt.md`** — Section J (Boundary Rules) body + END added (next → Golden Avatar™).
- **`prompts/golden-avatar-prompt.md`** — Section J (Boundary Rules) body + END added (next → $Million Moment™).
- Confirmed already-complete: `magic-formula-guide.html`, `magic-formula-prompt.md`, `million-moment-prompt.md`.
- **⚠️ Legacy file to retire:** `guides/million-story-guide.html` is an OLD style-demo guide (pre-™ naming, "Offer Matrix" without ™, self-labelled "exists to show the style system"). Superseded by the 3 part-guides. Recommend deleting it so it isn't mistaken for a client deliverable.

### ✅ DONE — Step 3: Red Diamond Offer™ · Part 1 · THE MONEY MODEL (Money Model + Offer Mechanics)
- **Structure (decided w/ Matthew 2026-07-08):** Red Diamond Offer™ = 2 parts. **Part 1 = Money Model + Offer Mechanics** (strategy). **Part 2 = Offer / Enrollment Document** (the asset — an offer doc that doubles as the enrollment doc).
- **Reference Matthew supplied:** the Fletcher Method / Customer Engine "Model Builder" prompt (`uploads/money-model-builder-prompt.txt`) + its guide (fletcher-method-system.vercel.app). This is the FORBIDDEN legacy brand — I did NOT find-and-replace it (that'd reproduce their copyrighted doc). Built an **original** Coach Launch asset that serves the same function, mapped onto our canon.
- **KEY DESIGN — mapped the generic money-model onto the $100K Day Accelerator™ (canon).** The Accelerator's 4 phases already ARE the money model: each phase = event format + ticket/VIP cash-in + close mechanism + offer price + revenue goal. So instead of Fletcher's 3 phases (Launch/Grow/Scale) or their two "paths" (membership vs high-ticket), the Money Model matches the client to their Accelerator phase (Launch/Execute/Growth/Mastery) and prices to that phase's canon band. Canon mapping used: MDM→$Million Promise™ · Product Roadmap→Magic Formula™ · Winning Workshop→Money Magnet Event · Ultimate Lead Magnet→Event Magnet™ · Ninja Content→nurture/Goliath Content™ · Cash Flow Engine™ = the ticket/VIP self-liquidation principle (full build is Step 5).
- **Builder prompt: ✅ DONE** — `prompts/money-model-prompt.md` ("The Money Model Builder — V1"). Full A–J, Builder/Gatekeeper mode, one-step-per-turn, math shown. Gates on Magic Formula™ (18+/25) + $Million Promise™. Flow: intake (name/prereq/promise/where-you-are/audience/budget/goal) → why models fail (3 patterns, original prose) → the Coach Launch model (immersion→conversion) → phase match → price (in phase band) → run the math (Low/Realistic/High, formulas shown) → **profitability gate: Realistic Day-7 return ≥ 1.0x or STOP** → offer mechanics (cash-in + enrollment by phase) → Money Model Report → hard stop → Part 2.
- **Companion guide: ✅ DONE** — `guides/money-model-guide.html`, built to the locked depth standard + visual system. Covers: what it is / is NOT · where it fits · how you'll build it · why models fail (3, deep) · the Coach Launch model · the 4-phase money-model table · pricing · the math + Day-7 gate (+ "why Day 7 not Day 90") · offer mechanics · a full worked example (Phase 2, flagged illustrative w/ general benchmarks) · hard rules · 6 common mistakes w/ fixes · Is-It-Ready checklist · next → Part 2. ™ verified; no legacy terms (grep-clean; legacy names appear ONLY in the prompt's TERMS NEVER TO USE block).
- **REFINEMENTS w/ Matthew (2026-07-08, same session):**
  - **Golden-rule clarification (Matthew):** the *builder prompt* must never invent frameworks/steps/numbers; the *guide* MAY give rich teaching context so the user understands the decision. Applied both docs accordingly.
  - **Event durations stacked** on a new line under each event name (Micro 60–90 · Single-Day 90–120 · Single-Day 180–240 · Multi-Day 60–180/day).
  - **Target conversion metrics + diagram added** to the guide (and prompt Section F): a CSS **conversion funnel** (Ad Spend → Registrants → Attendees → Sessions/Offers → Clients) + a **per-phase target table**.
  - **SHOW RATES = Matthew's REAL numbers (no longer generic), keyed to what they PAID to attend:** P1 free micro event 20–30% (a **bribe** lifts to ~50%) · P2 free seat ~20% / VIP 80–90% · P3 $27 ticket 70–85% / VIP higher · P4 $97 ticket **90–98%**. Guide has two callouts: "the bribe is your show-rate lever" and "the VIPs are the money."
  - **THE THREE CLOSES (Matthew's canon) — now an explicit choice that deepens by phase:** (1) **Enrollment Close** → One Day Offer, no call (P1); (2) **Strategy / Audit Close** → high-ticket, **free** early (P2) then **paid** (P3); (3) **Deposit Close** → $997 deposit, high ticket **without a call** (P4). User CHOOSES the close (default = phase's); rerun the model when the phase/close changes. NEVER invent a 4th close. Elevated in the guide (Offer Mechanics section, with a 3-close table) and the prompt (Section F "THE THREE CLOSES" block + rewritten Step 6 + report row).
- **⚠️ STILL FOR MATTHEW:**
  1. **Show rates = confirmed.** Still `[CONFIRM]`: **uptake + close rates** (per-phase) and **CPL** — currently general starting-points, flagged. Give real numbers when ready.
  2. **Naming:** tool is "the Money Model Builder" (house `[Asset] Builder` convention). Legacy tool was literally "Model Builder" — distinct but close; rename the asset if you want more separation.
- **Open (your call):** confirm uptake/close/CPL numbers; confirm whether a user can pick a non-default close for their phase (currently allowed) or must use the phase default only.

### ⚠️ STRUCTURE CHANGE — Red Diamond Offer™ is now 5 PARTS (was 4, was 2)
Matthew re-scoped Step 3 twice: to 4 parts (2026-07-09), then to **5 parts (2026-07-12)** with Cash Flow Max™ added. The Red Diamond Offer™ = **5 parts**, each its own builder prompt + guide:
1. **The Money Model** — ✅ DONE (Part 1).
2. **The SCORE™ Card** — ✅ DONE (Part 2).
3. **The Offer / Enrollment Document** — ✅ DONE (Part 3).
4. **The Event Magnet™** — ✅ DONE (Part 4) — the free lead magnet (seeded by the hot step of the Magic Formula™).
5. **Cash Flow Max™** — ✅ DONE (Part 5, 2026-07-12) — the PAID cash-flow assets (VIP upsell + order bumps) that raise cart value. Completes the Red Diamond Offer™ and the Offer Matrix™.
All repo files (both prompts, the money-model guide's tables, index.html) refer to the Money Model as "Part 1 of 2" in older copy — the NEW SCORE™ Card files say "Part 2 of 4". **Minor cleanup for later:** money-model-prompt.md + money-model-guide.html still say "2 parts / Part 2 = Offer Document" — update them to the 4-part structure when convenient (not urgent; they're not wrong about the Money Model itself).

### ✅ DONE — Step 3 · Red Diamond Offer™ · Part 2 · THE SCORE™ CARD (the master content source)
- **What it is:** the client's master content-source document — built on the **SCORE™ Framework** (Matthew's own IP): 5 acts, 19 blocks of format-neutral core persuasion copy. Build once, deploy everywhere (emails, posts, videos, ads, pages, pitches). Fletcher's equivalent is "SCRIPT" — a FORBIDDEN legacy term; we did NOT copy it (studied the uploaded `script-builder-prompt.txt` + guide for structure only, same call as the Money Model).
- **Source material (Matthew supplied, `uploads/`):** his own `score.md` (SCORE Framework™ 5-act system), `scorecard-template.md`, `builder-prompt.md` (his V1 test), `example-scorecard-output.md`, `scorecard-guide.html`. These were TEST files predating current canon — adapted, not used as-is.
- **DECISIONS LOCKED with Matthew (2026-07-09):**
  1. **Naming:** **SCORE™ Card** (™ on SCORE) built on the **SCORE™ Framework**. The 5 act words (Serendipity/Connection/Opportunity/Reality/Execution) and the 19 block labels stay PLAIN (no ™) — like PIVOT/FOCUS.
  2. **Scoring:** NO /25. Qualitative **per-act Ready Check** + a specificity enforcer (same precedent as $Million Moment™). Reason: the 19 blocks are creative copy assembled from upstream assets that are ALREADY scored/locked, so the gate is pass/fail (true · specific · format-neutral · from locked inputs), not a rubric.
  3. **Avatar:** ONE **combined** SCORE™ Card (speaks to the shared pain/desire across avatars; note a per-avatar card can come later).
- **KEY DESIGN — assembles from locked upstream, doesn't re-collect.** Because it now sits AFTER the Money Model, the card pulls: S-1 anchor ← $Million Promise™ (verbatim) · avatar + C-1 "3 Problems" ← Golden Avatar™ (one problem per Magic Formula™ stage) · **C-2 story ← $Million Moment™** (Matthew's canon name for what Fletcher calls "System Discovery Story") · O-1 System Reveal ← Magic Formula™ 3×3 (all 9 steps) · O-2 ← core differentiator · **E-5 CTA ← the locked Money Model** (price + one of the Three Closes). Only 4 things are collected fresh (not in upstream): credibility anchors, client proof, urgency/scarcity, result timeline.
- **19 blocks:** S (4): $Million Promise™ Anchor · Question · Symbolism · Debunking. C (3): 3 Problems · $Million Moment™ Story · Credibility Anchors [+ Universal Gap bridge]. O (3): System Reveal · Core Differentiator · Client Proof [+ results disclaimer]. R (4): Consequence of Inaction · Limits · Reward · Desire. E (5): 2 Ways · Hard Choice · Because · FOMO · CTA.
- **Folded in from Fletcher (rewritten original, NOT copied):** the **Mirror Block** technique (S act — "last stop on your journey"), the **Universal Gap** bridge (end of C → into O), a **results/earnings disclaimer** line under O-3, and CTA-forks-by-close (ours uses the canon **Three Closes** from the Money Model, richer than Fletcher's binary).
- **Builder prompt: ✅ DONE** — `prompts/score-card-prompt.md` ("The SCORE™ Card Builder — V1"). Full A–J. Builder/Enforcer voice mode. Extract→Build→Assemble; one ACT per turn; all 19 blocks in full; format-neutral (except E-5); gates on $Million Story™ (all 3 parts) + Magic Formula™ (18+) + locked Money Model. Ready Check in Section H; assembled-card output spec in Section I; hard stop → Part 3. Stripped ALL legacy stats (his test used 22 yrs / 226 events / $24.6M) → canon **$24M + $5.2M only**. Fixed pillar names to canon (Offer Matrix™/Money Magnet™/Client Flywheel™). Legacy terms (SCRIPT, MDM, Fletcher, Aaron, Customer Engine, Model Builder, webinar, funnel, etc.) appear ONLY in the Section F "TERMS NEVER TO USE" block.
- **Companion guide: ✅ DONE** — `guides/score-card-guide.html`, built to the locked depth standard (`million-promise-guide.html`) + locked visual system (`assets/coach-launch-guide.css`). Covers: what it is / is NOT · where it fits (4-part table, "you are here" on Part 2; what feeds in; what breaks if weak) · how you'll build it (extract/build/assemble) · the 5 acts overview (cards) · a full deep-dive per act with every block + ✓vs✕ · the Mirror Block + Universal Gap callouts · the disclaimer · the format-neutral rule · a worked before→after (illustrative productivity-coach example, flagged, no fabricated CL results) · hard rules · 6 common mistakes w/ fixes · "Is It Ready?" (explains WHY no score + per-act Ready Check) · green Remember quote + generic CTA card (SCORE™ Card Builder / Join $100K Day Accelerator™ → fast.coachlaunch.com). ™ verified; no legacy terms.
- `index.html` updated: Red Diamond Offer™ section now says "4 parts", Part 2 guide + builder cards live, growing-note updated.

### ✅ DONE — Step 3 · Red Diamond Offer™ · Part 3 · THE RED DIAMOND OFFER™ ENROLLMENT DOC
- **What it is:** the single written offer that packages the locked Money Model and doubles as the asset that enrolls the client — a sales letter that works 24/7. Dropped in the event chat, emailed to no-shows, sent to "let me think about it" people. Always represents the COMPLETE Red Diamond Offer™ (never one step, never the Event Magnet™ lead magnet). Fletcher's equivalent is the "Enrollment Amplifier" — a FORBIDDEN legacy term; studied the uploaded `ea-builder-prompt.txt` + guide for structure only, did NOT copy (same call as the Money Model + SCORE™ Card).
- **DECISIONS LOCKED with Matthew (2026-07-09):**
  1. **Naming:** **"Red Diamond Offer™ Enrollment Doc"** — leans on the existing Red Diamond Offer™ ™; no new coined name. Tool = "The Red Diamond Offer™ Enrollment Doc Builder."
  2. **Validation:** completion gate (refuse to assemble until every required offer block exists) + qualitative per-section **Ready Check**. **No /25** — same precedent as the SCORE™ Card + $Million Moment™ (this is assembled creative copy from already-locked/scored upstream).
- **⚠️ REBUILT TO V2 (2026-07-09, same session)** — see the header note + the 7X Red Diamonds / 3 Levels canon block above. Matthew flagged that V1 (below) leaned too hard on the Fletcher reference, under-taught the actual offer-document structure, and completely MISSED the 7X Red Diamonds (his core "how to add value to a high-ticket offer" framework). **V2 decisions locked with Matthew:** (1) it's a **LONG-FORM SALES LETTER** (~2,000–3,000 words), not the tight one-pager; (2) built on his **10-part Red Diamond Offer structure** with the **7X Red Diamonds + 3 Levels** as the centerpiece value layer; (3) **KEEP** the DR blocks (Fork · Confidence Filter · P.S.) since it's a sales letter; (4) the Total-Value/Price box is the **Value Stack** (rename, to avoid colliding with "Offer Mechanics" = cash-in+close in the Money Model); (5) **must follow the SCORE™ 5-act arc** (S→C→O→R→E) — Matthew's directive that this is the psychological flow behind ALL Coach Launch copy going forward. Offer layer seats INTO the arc: 7X Red Diamonds + 3 Levels in **Opportunity**, Value Stack in **Reality**, bonus/Fork/Confidence Filter/CTA/Objection Crusher/FAQ/guarantee/P.S. in **Execution**. Added **Fast Acting Bonus** + **FAQ**. Prompt = V2; guide rebuilt to match (teaches every offer-doc part + a full 7X Red Diamonds deep-dive + the 3 Levels). Files re-verified: A–J complete, legacy-clean, ™ correct (Red Diamond / Level / offer-block names carry NO ™).
- **KEY DESIGN — the SCORE™ Card made this a clean 1:1 with the reference.** The Fletcher EA is "SCRIPT-first" (reads a pre-built master-copy doc + Model Builder, arranges + adds Content Blocks). Our SCORE™ Card is exactly that master-copy layer, so the Enrollment Doc reads the **SCORE™ Card (Part 2) + the Money Model (Part 1)**, arranges the narrative blocks into the SCORE™ arc, and generates the offer layer (7X Red Diamonds, 3 Levels, Fast Acting Bonus, Value Stack, Fork, Confidence Filter, Objection Crusher, FAQ, Risk Reversal, CTA, P.S.). *(V1 note, superseded: originally framed as arranging 19 blocks into a generic offer order + a small DR block set.)*
- **Reused (arranged verbatim, never rewritten) vs. built here:** Reused = S-1/S-2/S-3 (headline+mirror) · C-1/S-4/Universal Gap (challenges+gap) · C-2/C-3 (story+credibility) · O-2/O-1 (differentiator + full 9-step system) · O-3 (proof+disclaimer) · R-1/R-3/R-4/R-2 (stakes) · E-1/E-2/E-3/E-5 (close beats + price/close) · E-4 (FOMO). Built here = **Value Stack · Fork in the Road · Confidence Filter · Objection Crusher · Risk Reversal (optional) · P.S./P.P.S.** (standard direct-response elements, unbranded/no ™).
- **CTA adapts to the locked close (canon Three Closes):** Enrollment / Strategy-Audit (free or paid) / Deposit — richer than Fletcher's A/B/C. Rule 10 adapts the reference's "always present price live" for our **no-call closes** (P1 Enrollment, P4 Deposit): present the offer at the event; the doc amplifies + catches considerers; for no-call closes the doc carries more weight but still runs alongside a real event.
- **Builder prompt: ✅ DONE** — `prompts/enrollment-doc-prompt.md` ("The Red Diamond Offer™ Enrollment Doc Builder — V1"). Full A–J. Builder/Enforcer voice. Confirm→build offer layer→assemble; one section per turn; all blocks in full; gates on the locked SCORE™ Card + Money Model. Completion gate + Ready Check in Section H; assembled-doc output spec + reading-order map in Section I; hybrid (two-reader) formatting + ~2,000–3,000-word target; hard stop → Part 4. Legacy terms (Enrollment Amplifier, SCRIPT, MDM, Fletcher, Aaron, Customer Engine, Model Builder, webinar, funnel, etc.) appear ONLY in the Section F "TERMS NEVER TO USE" block.
- **Companion guide: ✅ DONE** — `guides/enrollment-doc-guide.html`, built to the locked depth standard (`million-promise-guide.html`) + locked visual system (`assets/coach-launch-guide.css`). Covers: what it is / is NOT · where it fits (4-part table, "you are here" on Part 3; what feeds in/out; what breaks if weak) · how you'll build it · **the reused-vs-built map (table)** · the offer layer deep-dive (each block: what it is / why it matters / how to find yours / ✓vs✕) · CTA-by-close table · the hybrid two-reader formatting + length · the deployment rule (present live, doc amplifies; no-call adaptation) · a worked before→after (illustrative business-coach Value Stack + Fork, flagged, no fabricated CL results) · hard rules · 6 common mistakes w/ fixes · "Is It Ready?" (completion gate + per-section Ready Check, why no score) · green Remember quote + generic CTA card (Red Diamond Offer™ Enrollment Doc Builder / Join $100K Day Accelerator™ → fast.coachlaunch.com). ™ verified; no legacy terms.
- `index.html` updated: Red Diamond Offer™ section now says Parts 1–3 live / Part 4 in build; Part 3 guide + builder cards live; growing-note updated.

### ✅ DONE (2026-07-09) — `dashboard.html` — the Training Portal
- **What it is:** a client/student-facing **training portal** (separate from `index.html`, Matthew's internal hub). Presents every guide + builder prompt **organized in build sequence** (Step 1 Parts 1–3 → Step 2 → Step 3 Parts 1–4 → Steps 4–9 placeholders → internal reference), for **training + download**.
- **Features:** sticky step nav · 3-pillar/9-step visual roadmap · "how to use" 01-02-03 strip · per-module cards each with **Guide (Open ↗ / Download)** and **Builder Prompt (Copy prompt / Download .md)**. All 7 builder prompts are **embedded in the HTML** so Copy + Download work whether the file is hosted (GitHub Pages) OR opened locally from disk (no CORS/fetch dependency). Part 4 (Event Magnet™) + Steps 4–9 show as greyed "Coming soon / In build" placeholders so the full journey is visible.
- **Scope call (Matthew, 2026-07-09):** include EVERYTHING — client deliverables AND the internal style/voice guides + template. Internal items sit in a clearly-separated **"Internal Reference — team use only, not client deliverables"** section (preserves the canon internal/client split while keeping one dashboard).
- **Build:** generated by `outputs/build_dashboard.py` (reads the prompt .md files, injects them as JSON). Uses the locked `assets/coach-launch-guide.css` + brand hero/CTA. Verified: 12/12 links resolve, prompt JSON parses, ™ + brand correct.
- *Note:* it links the Step-1 Part-1 prompt at `prompts/million-promise-prompt.md`; the **Download** button also saves it as `million-promise-prompt.md`.
- **⚠️ UPDATE (2026-07-11) — 3-PILLAR RESTRUCTURE + CLICKABLE MAP.** The top roadmap is now the shared **Formula map** (`.map dash-map`) with clickable steps that deep-link to each module section. The body is delineated into **3 clear pillar parts** via `.pillar-banner` headers (Pillar #1 Offer Matrix™ / #2 Money Magnet™ / #3 Client Flywheel™); `#upcoming` was split into `#money` (4–6) and `#clients` (7–9). Nav → pillar anchors. `.pillar-banner` + `.dash-map` CSS live inline in `dashboard.html`'s `<style>`; the `.map` CSS itself is inherited from `assets/coach-launch-guide.css`. Since the original `build_dashboard.py` generator no longer exists in the repo, dashboard.html is now maintained by **direct in-VM Python edits** (back up first; verify anchors + `#prompt-data` JSON + 0 NULs).
- **HERO + HEADLINES (2026-07-11):** top **key headline = "The $100K Day Accelerator™"** (hero `guide-name`); the hero pill is now just "Training Portal". Directly above the formula map: headline **"The $100K Day Formula™"** (`.formula-headline`) + sub-headline (`.formula-subhead`): *"How To Run A $100K Day Masterclass and Close 8–12 High Ticket Clients Every 28 Days, Without One-on-one Sales, Events That Flop, Or Chasing a Broke Audience."* The sub-headline is forced to **exactly 2 lines on desktop** via a desktop-only `<br class="sub-br">` at the balance point (after "…Every 28 Days,") + a desktop font cap of 0.98rem so neither line overflows the ~812px content width; on mobile the `<br>` is hidden and it wraps naturally (`text-wrap:balance`).
- **AI AGENTS section added at the bottom (2026-07-11):** new `#agents` section (before the closing CTA) — a visual placeholder lineup of **6 "run-the-machine" agents** (functional names, to be branded later; each a `.agent-card` with an inline SVG icon + "Coming soon" tag; responsive 3→2→1 grid): **1) Social & Authority · 2) Show-Up & Follow-Up · 3) Enrollment & Objection · 4) Ad Optimization · 5) Metrics & Money-Model · 6) List Reactivation.** Concept (Matthew): the builder prompts/guides BUILD the offer (Pillar 1); these AGENTS run the ongoing marketing machine (fill → grow → attend/follow-up → close → measure → reactivate). **Visual only for now — the agents themselves get built later.** Added "⚙ Agents" nav link. CSS (`.agents-*`, `.agent-*`) lives inline in dashboard.html's `<style>`.
- **AGENTS given CHARACTERS (2026-07-11):** each agent is now a named team member with a **profile pic**, mixed male/female. Avatars are **self-contained inline SVG portraits** (flat-illustration style, varied skin tones / hair / glasses / beard — no external images, offline-safe). The card leads with the person's **name** (`.agent-name`) + the function as a crimson **role** subtitle (`.agent-role`), inside a circular `.agent-avatar`. Header reworded to "Meet Your Agent Team". Lineup: **Maya** (Social & Authority) · **Leo** (Show-Up & Follow-Up) · **Sofia** (Enrollment & Objection) · **Marcus** (Ad Optimization) · **Priya** (Metrics & Money-Model) · **Jack** (List Reactivation). Names are provisional. Avatar generator kept at `/tmp` this session (`gen_avatars.py`) — if regenerating, re-embed the SVGs; they’re currently hard-coded into dashboard.html.
- **PHOTO-REAL upgrade wired (2026-07-11):** each `.agent-avatar` now layers a photo `<img class="agent-photo" src="assets/agents/<name>.jpg" onerror="this.remove()">` OVER the inline SVG, so photos show when present and the SVG shows as automatic fallback when not. Photos go in `assets/agents/` named `maya/leo/sofia/marcus/priya/jack.jpg`. **Generation can't run in the Cowork sandbox** — its egress is allowlisted and `api.kie.ai` is blocked (proxy 403; PyPI etc. work). So generation is delegated to the user's machine via **`scripts/generate_agent_photos.ps1`** (PowerShell, Windows-native, no Python/Node needed): prompts for the Kie.ai key (NOT stored), calls `google/imagen4` (1:1, relaxed-modern warm style), polls `recordInfo`, downloads the 6 jpgs into `assets/agents/`. Manual alternative: drop any 6 square jpgs with those names. See `assets/agents/README.md`. Kie.ai API shape (for reference): POST `api.kie.ai/api/v1/jobs/createTask` {model,input:{prompt,negative_prompt,aspect_ratio,seed}} → GET `/api/v1/jobs/recordInfo?taskId=` → data.state + data.resultJson.resultUrls[0].

### ✅ DONE (2026-07-09) — BRAND FOUNDATIONS (3 new client-facing tools, built BEFORE the Formula)
- **Decision (Matthew, 2026-07-09):** the dashboard's old "reference section" becomes 3 real client-facing tools — each a builder prompt + companion guide — and the **New Guide Template** card is removed. Scope = "everything" but as a client/student **training portal**; placement = these 3 **replace the reference section**. I placed them as a **"Brand Foundations"** section AHEAD of Step 1 (portal runs in build-order) and retired the internal style/voice guides + template FROM THE DASHBOARD (the files still exist on disk in `style-guide/` + `templates/`; just not surfaced in `dashboard.html`).
- **The 3 Foundations (built in order; names carry NO ™ — descriptive, like the old internal docs):**
  1. **Business Brand Profile** — `prompts/brand-profile-prompt.md` + `guides/brand-profile-guide.html`. Identity layer (what-you-do one-liner, mission, values, positioning, personality/archetype, **brand promise**) + business snapshot (offer, model, market, **Accelerator phase**, goals). **Brand + business snapshot** per Matthew.
  2. **Visual Style Guide** — `prompts/visual-style-prompt.md` + `guides/visual-style-guide.html`. Feeling → one hero color + neutrals (hex) → two fonts + size ladder → logo/wordmark rules → imagery/texture → components → consistency rules. Grounded in the Profile's personality.
  3. **Brand Voice** — `prompts/brand-voice-prompt.md` + `guides/brand-voice-guide.html`. Voice attributes (means/doesn't-mean) → tone range (sell/teach/support/celebrate) → signature style → words you use/avoid → grammar & formatting → do-this/not-that → gold-standard sample. Best input = a real writing sample.
- **Standards met:** all 3 prompts = full **A–J** (verified 10/10 sections each), one-step-per-turn, Builder/Coaching mode, **Ready Check (no /25)** — same no-score precedent as $Million Moment™/SCORE™ Card (foundations = truth-about-the-business, not a conversion asset). All 3 guides built to the locked depth standard (`million-promise-guide.html`) + visual system. ™ correct (canon names ™'d, foundation names NOT ™'d — verified), legacy terms only inside the TERMS NEVER TO USE blocks (verified), green Remember quote + generic CTA card on each guide.
- **Canon care:** the Formula's first *step* is still the **$Million Promise™** — the Profile does NOT replace it. Explicit distinction drawn in both the Profile prompt + guide: **brand promise** (the experience standard, in the Profile) ≠ **$Million Promise™** (the marketing hook, Step 1). Framed as a Foundation *layer* the Formula sits on, not a reordering of the 9 steps.
- **`dashboard.html` rewired** via `outputs/build_dashboard.py`: added the Brand Foundations section + roadmap "◆ Brand Foundations · start here" lead-in + nav link; embeds all **10** prompts now (7 Formula + 3 Foundations) for offline copy/download; removed template + internal-reference cards. Verified: 11/11 local links resolve, 10 copy + 10 download buttons, prompt JSON parses, template/style-guide/Teaching-Voice refs all 0.
- **⚠️ Truncation gremlin recurred:** the Write tool + bash `/sessions/…` mount read-lag truncated `build_dashboard.py` on save (as PROGRESS warned before). Workaround that worked: wrote the generator to the VM's local `/tmp/gen.py` and ran it there (reads the complete prompt .md files from the mount, writes `dashboard.html` back to the mount). Keep this trick for future large generator runs.
- **Note for Matthew:** `index.html` (internal hub) still shows the OLD internal-reference + template group and does NOT yet list these 3 Foundations — mirror the change into `index.html` when convenient (I focused on `dashboard.html` as asked).

### ✅ DONE (2026-07-10) — Step 3 · Red Diamond Offer™ · Part 4 · THE EVENT MAGNET™
- **What it is:** the free, VISUAL lead magnet — ONE hot step of the Magic Formula™ turned into a tangible tool (cheat sheet / template / checklist / script / calculator / scorecard / one-page map / audit / swipe file) that a stranger uses in one sitting, opens a curiosity loop, and raises their hand to attend the event. It's the FUSE. Fills the Money Magnet™ events (Pillar 2). Completing it **finishes the Red Diamond Offer™ (all 4 parts) and the whole Offer Matrix™ (Pillar 1).**
- **Reference (Matthew supplied):** the legacy "lead magnet builder" prompt (`uploads/lead-magnet-builder-prompt.txt`) + its guide (fletcher-method-system.vercel.app/Guides/lead-magnet-guide.html). FORBIDDEN legacy brand — studied for STRUCTURE ONLY, built an original Coach Launch asset (same call as the Money Model / SCORE™ Card / Enrollment Doc). Legacy terms live ONLY in the TERMS NEVER TO USE block.
- **DECISIONS LOCKED with Matthew (2026-07-10):**
  1. **Reference:** Matthew supplied the examples; build from them (structure only).
  2. **Scoring:** "follow what the example recommends." The example validates with a **5-criteria pass/fail gate (OPTIN), NOT a /25.** So the Event Magnet™ uses a qualitative pass/fail gate too — same precedent as the $Million Moment™ / SCORE™ Card / Enrollment Doc. Reason: the hot step (scored/locked in the Magic Formula™, 18+/25) and the $Million Promise™ (scored/locked) it's built on were ALREADY scored — so this is a pass/fail assembly gate, not a rubric.
  3. **Deliverables:** both prompt + guide.
- **KEY DESIGN — canon mapping (Fletcher → Coach Launch):** Lead Magnet → **Event Magnet™** · MDM → **$Million Promise™** (title format) · Product Roadmap → **Magic Formula™** (the 3×3 the hot step comes from) · Model Builder → **Money Model** (sets the CTA's event + close) · Hot Step → the plain **hot step ALREADY flagged in the Magic Formula™** (this tool CONFIRMS it, doesn't hunt from scratch — a clean canon efficiency) · OPTIN → an **original 6-Point Event Magnet™ Check** (pass/fail: On-System · Visual · A Tool Not a Teach · Quick Win · Incomplete by Design · Leads to the Event) · CTA path (Zero Selling / Community QuickStart) → **the CTA points to the Money Magnet™ EVENT, framed by the Accelerator phase + the Money Model's chosen close** (never a "free call").
- **⚠️ TWO CANON CALLS I MADE (flagged for Matthew to confirm/override):**
  1. **No "Authority Amplifier" coined.** Fletcher's "TNT = Tool + Training" pairs the lead magnet with a branded Authority Amplifier video. Per the golden rule I did NOT invent a new Coach Launch tool name. Instead I taught the pairing UNBRANDED ("pair it with a short welcome video/note; never hand it over cold") and pointed the full follow-up/nurture to the canon **Genie X Converter™** (Step 6). *If you want a coined name for the pairing asset, tell me and I'll wire it in.*
  2. **Dropped the cost-per-lead stats** ($1.80–$3 vs $10+). Those are the legacy brand's results, not ours — golden rule (only $24M/$5.2M). Taught the visual-first principle without the borrowed numbers. *If you have your OWN CPL numbers, give them and I'll add them.*
- **Builder prompt: ✅ DONE** — `prompts/event-magnet-prompt.md` ("The Event Magnet™ Builder — V1"). Full A–J (10/10 verified). Coaching Builder voice, one-step-per-turn. Gates on the Magic Formula™ (18+/25, hot step flagged) + $Million Promise™; Money Model recommended (for the CTA). Flow: opening → intake/extract → Step 1 lock foundation → Step 2 confirm the hot step (re-pick or Full System Reveal option) → Step 3 pick the visual format (tool not teach) → Step 4 title in $Million Promise™ format → Step 5 build the spec (what's on it / how it looks / one-sitting win) → Step 6 the 6-Point Check (pass/fail, all six) → Step 7 CTA-to-event + pairing + use-the-visual-everywhere → Final Output → hard stop. Two versions supported: One-Step (default) + Full System Reveal (exception, must justify).
- **Companion guide: ✅ DONE** — `guides/event-magnet-guide.html`, built to the locked depth standard (`million-promise-guide.html`) + visual system (`assets/coach-launch-guide.css`). Covers: what it is / is NOT (fuse not payload; tool not teach) · the visual "secret sauce" · where it fits (4-part table, "you are here" on Part 4; what feeds in/out; what breaks if weak) · how you'll build it · the two versions · finding your hot step (already flagged — confirm) · the formats (approved vs rejected + one-sitting test) · the title-is-the-ad (promise format, ✓vs✕) · the 6-Point Check (why no score + pass/fail table) · the CTA (by phase, never a free call, pair it, use the visual everywhere) · a full worked before→after (27-page LinkedIn "guide" → "The 7-Touch DM Sequence" one-page script, flagged illustrative) · hard rules · 6 common mistakes w/ fixes · Is-It-Ready checklist · green Remember quote + generic CTA card (Event Magnet™ Builder / Join $100K Day Accelerator™ → fast.coachlaunch.com). ™ verified; no legacy terms; no CPL stats.
- `index.html` updated: Red Diamond Offer™ section now "all 4 parts live / Offer Matrix™ complete"; Part 4 guide + builder cards live; growing-note → next = Step 4 · The Sniper Close™.
- **✅ dashboard.html rewired (2026-07-10):** the Part 4 "coming soon" placeholder is now a **live module card** (Guide Open/Download + Prompt Copy/Download), and the Event Magnet™ prompt is embedded as the **11th** prompt in the `#prompt-data` JSON block so Copy/Download work hosted OR offline. Done via an in-VM Python edit (parse the JSON block → add `event-magnet-prompt` key → swap the placeholder card for the live-card template copied from the Part 3 card). Verified: JSON valid (11 keys), 11/11 guide links resolve on disk, 11/11 `data-copy`/`data-dl` buttons map to embedded keys, placeholder gone.

### ✅ DONE — Step 4 · The Sniper Close™ (Pillar 2 · Money Magnet™)
- The Offer Matrix™ (Pillar 1) is COMPLETE. **Step 4 is now the first LIVE step of the Money Magnet™**: Step 4 · The Sniper Close™ ✅ → Step 5 · Cash Flow Engine™ ⬜ → Step 6 · Genie X Converter™ ⬜.
- **Design LOCKED + BUILT (2026-07-14) — full build detail in the header entry above.** In short: tool = **Sniper Close™ Builder** → outputs the **Sniper Presentation™** (full live pitch) ending in the **Sniper Close™** — ONE flow. Spine = **SCORE™** acts mapped to Matthew's 4 card phases (WHO/WHAT/WHY/WHEN). Flex axes = **Format** (Masterclass single-day vs Challenge 3-day) + **Close** (Enrollment / Strategy free-or-paid / Deposit). It ARRANGES the locked SCORE™ Card + Money Model + Red Diamond Offer™ into a live pitch, and pulls stories from the `bits/` library (a story-bit slot in every act).
- **✅ Builder prompt:** `prompts/sniper-close-prompt.md` — full A–J (10/10), one-step-per-turn, gated on SCORE™ Card + Money Model + Red Diamond Offer™ + $Million Story™ + Magic Formula™; branches on Format + Close; VIP-Hour block (Growth+, start-or-end); integrates the bits/ library; validation = pass/fail 8-point Sniper Presentation™ Ready Check (NO /25). Legacy-clean; 239 ™.
- **✅ Companion guide:** `guides/sniper-close-guide.html` — locked depth (13 H2 sections) + visual standard; Formula map stamped Step 4; green Remember + generic CTA. Verified 1 map, 0 NULs, 74 ™.
- **✅ Wired:** dashboard.html Step 4 → live module card + prompt embedded as the 13th `#prompt-data` key (embedded==disk, 13 keys, all buttons mapped, 13 guide links resolve, ™ preserved, 0 NULs). index.html Step 4 section added (20 links resolve) + Growing note → next Step 5. Map script already had `sniper-close`→Step 4.
- **✅ bits/ Speaker Bit Library** seeded earlier this session (README + template + 10 clean-room bits + 7 of Matthew's examples repurposed) — the story layer the presentation draws from.
- **⚠️ Remember for Step 5 · Cash Flow Engine™:** build the **thank-you / landing-page video** asset (post-registration; VIP-upsell variant + plain thank-you/show-up variant) — decided this session to house it with Step 5, NOT Step 4.

### ✅ DONE (2026-07-14) — THE PRESENTATION ENGINE™ (production tool · pairs with Step 4)
- **What it is:** a **production tool** (NOT a Formula step 1–9) that turns a finished asset — first and foremost the **Sniper Presentation™ Script** (Step 4) — into an on-brand HTML slide deck the client presents from their own laptop. Two pieces: **The Presentation Engine™ Builder** (the brain) + the **Slide Viewer** (`slide-viewer.html`, one double-click offline file). Modeled on Matthew's supplied reference (Fletcher's "AI Presentation Engine" — guide + Slide Builder `SKILL.md` + `slide-viewer.html`); studied for structure only, rebuilt original in canon, legacy stripped.
- **THE KEY DESIGN — SLIDE vs SPEAKER SCRIPT.** The Sniper Presentation™ Script already gives each beat a `SLIDE:` (→ clean audience slide), a `SPEAKER SCRIPT:` (→ presenter-only notes), and PHASE/ACT (→ run-sheet). So the first-class path is a near-1:1 beat→slide conversion, not design-from-scratch, and the talk track never lands on the audience slide.
- **THE 4 IMPROVEMENTS over the reference (all built):** (1) **Presenter View** — `P` pops out a second-screen window (speaker script + next-slide preview + timer + phase/act), `S` = in-window notes panel, `T`/`R` = timer; run-sheet drawer grouped by phase. (2) **Auto-brand** from the Business Brand Profile + Visual Style Guide → slide `:root` tokens. (3) **SCORE™/Sniper slide-type library** + distinct story-bit slides. (4) **Reusable saved-slide templates** (cover/bio/value-stack/CTA) so slide work compounds.
- **Files:** `slide-viewer.html` (rebuilt/improved; `node --check` clean; SLIDES = `{type,label,phase,act,notes,html}`; **ships with an 8-slide SAMPLE DECK (upgraded from 3 at Matthew's request) spanning 8 layout models — cover/promise · qualify (for-you/not-for-you) · story-bit · framework (Magic Formula™ 3×3) · Hell→Heaven split-comparison · belief→truth rows · offer value-stack · CTA close — TWO of them (story-bit + CTA) demonstrate the hybrid IMAGE option with offline base64 inline-SVG backgrounds**; **ALIGNMENT STANDARD (Matthew 2026-07-14): slide content is CENTERED (vertical + horizontal) — eyebrow/headline/body/CTA — EXCEPT bullet lists + line-item rows, which sit LEFT-aligned inside a centered max-width `.block`; card/column titles stay centered. Baked into the builder prompt's SLIDE RULES + skeleton (`.slide` centers; `.block` = centered wrapper with `text-align:left`) and applied to all 8 sample slides; dashboard re-embed synced (embedded==disk)**; **+3 IMAGE LAYOUTS added (Matthew 2026-07-14) → 11-slide sample: (9) intro-split = founder photo one side + system-name headline + $Million Promise™ subhead + system image; (10) image-points = side image + headline + exactly 3 bordered points; (11) image-hero = full-bleed image + a dark headline box for ONE key point. All use offline base64 inline-SVG PLACEHOLDERS labelled "Founder photo / System image / Image / Full-bleed image" (no Kie.ai key was in-session; sandbox also can't download generated image bytes to inline — real images get dropped in on the user's machine via the `kie-image-gen` skill). The 3 new types are registered in the prompt's slide-type library + ADAPT PER TYPE. **REAL IMAGES GENERATED + EMBEDDED (2026-07-14): Matthew supplied his Kie.ai key; generated 4 placeholder images via `google/nano-banana` (founder headshot, system diagram, left-column photo, full-bleed hero). Sandbox STILL can't download the CDN bytes (403 through the proxy) — SOLVED by running a PowerShell downloader on the REAL machine via the Desktop Commander MCP (`scripts/_fetch_deck_imgs.ps1` → `assets/deck-samples/*.png`), which the sandbox then reads from the mount and base64-inlines into the viewer.** So slides 9/10/11 now carry real photoreal PNGs (offline, self-contained); slides 3/8 keep abstract inline-SVG backgrounds. slide-viewer.html is now ~6.7MB (4 PNG data URIs + 2 SVG). JS `node --check` clean; 0 NULs. **LESSON (confirmed again): Kie generates fine from the sandbox but image-CDN download is proxy-blocked — use Desktop Commander (real machine) to fetch, then read via the mount.** **REVISIONS (Matthew, same session): (1) intro-split moved to SLIDE 1 (deck now opens on it); array re-glued with commas + comments renumbered 1–11. (2) founder regenerated as a waist-up, arms-crossed half-body shot (light-gray studio bg) and the left column switched to `#ececee center/contain` so the founder is NEVER cropped (hands/arms visible); re-fetched via Desktop Commander + re-embedded. (3) slide-11 (full-image + box) box re-centered vertically+horizontally with a uniform 45% dark overlay so it's always visible. JS clean; 0 NULs; ~6.7MB.** **(4) Founder re-done AGAIN: front-facing (looking straight at camera), arms crossed, generated on plain white, then BACKGROUND REMOVED to transparent via Pillow corner flood-fill (`assets/deck-samples/founder_cutout.png`, ~66% transparent) — embedded as a transparent PNG cutout on the light-gray column, positioned `center bottom` so she's grounded. ~6.3MB.** **(5) Fixed a CSS bug (double `))` in the founder `.left url()`) that had blanked the founder; added `grid-template-rows:100%` so she fills the column. (6) Built a GENERALIZED "9-step system" graphic (inline SVG, `assets/deck-samples/system9.svg`) — 3 generic STAGE columns × 9 STEP cards, each with its OWN distinct crimson icon (idea/target/flag/speech/$/gear/doc/people/growth) + placeholder name bars, NO branded names — and embedded it as the intro slide's system image (contain). Deck ~5.2MB.** **(7) CANON (Matthew): the intro-split's right-hand image = the **Event Magnet™** (Red Diamond Offer™ · Part 4) — the free visual hot-step tool is the event's CORE HOOK, so it opens the deck (the same asset that filled the room, now on stage). Updated the builder prompt (intro-split def in the slide-type library + ADAPT PER TYPE + a RECOMMENDED prerequisite) and the sample slide-1 notes/label to call it the Event Magnet™; the generalized 9-step system map stays as the stand-in until a real Event Magnet™ exists. dashboard re-embed synced. ⚠️ **LESSON: the Edit tool TRUNCATED the ~5MB slide-viewer.html tail (lost the engine JS + closing tags) — repaired in-VM Python. DO NOT use Edit/Write on slide-viewer.html once it's image-heavy; make changes via in-VM Python string-ops to the mount.** **(8) Reordered: offer/value-stack moved to the LAST slide (11); full-bleed (now slide 10) headline box dropped to ~¼ up from the bottom (`align-items:flex-end;padding:6vh 6vw 15vh`). New order: Intro · Cover · Qualify · Story-bit · Framework · Split · Belief · CTA · Image-points · Full-bleed · Offer-stack. Full-bleed box finalized at `bottom:15%` (absolute, centered — Matthew approved).**  **(9) PACKAGED: added a **Slide Viewer** action (Open/Download `slide-viewer.html`) to the Presentation Engine™ card on dashboard.html AND a Slide Viewer card in index.html — so the full kit (Guide + Builder Prompt + Slide Viewer) is downloadable from the portal. Verified: dashboard 14 keys + all guide links resolve + slide-viewer linked; index 27/27 links resolve; 0 NULs both. NOTE: site is NOT hosted yet (no live URL) — publish via GitHub Pages to get a shareable dashboard link.**), `prompts/presentation-engine-prompt.md` (full A–J 10/10; images via the existing **`kie-image-gen` skill**; validation = pass/fail **Quality Check, NO /25**; legacy-clean; 118 ™; 0 NULs), `guides/presentation-engine-guide.html` (locked depth+visual, 12 H2 sections; **CARRIES THE STANDARD FORMULA MAP** — every training-guide header must match (Matthew, 2026-07-14). Stamped via `apply_formula_map.py`, flagging **Step 4 · The Sniper Close™** as "You are here" (the step it serves) + a custom intro clarifying it's a production tool, not a numbered step; `presentation-engine`→Step 4 registered in the map script. 62 ™; 0 NULs).
- **DECISIONS (Matthew, via AskUserQuestion):** name = **The Presentation Engine™**; scope = **any deck, first-class Sniper Presentation™ path**; all 4 improvements; house packaging (prompt + guide + dashboard/index wiring).
- **WIRED:** dashboard.html — live **"Production Tool"** module card after the Step 4 card in #money; prompt embedded as the **14th** `#prompt-data` key (embedded==disk; 14 keys; all data-copy/data-dl map; 14/14 guide links resolve; ™ 1815→1935; 0 NULs). index.html — new Production-Tool section (22/22 links resolve) + Growing note updated. Backups `dashboard.html.bak-preengine` + `index.html.bak-preengine`.
- **™ RULE / CANON:** The Presentation Engine™ + Sniper Presentation™ carry ™; the SLIDES carry the CLIENT's brand/voice, not Matthew's. Doesn't write the asset (that's The Sniper Close™ Builder / the matching builder) and doesn't build pages/funnel/follow-up (Cash Flow Engine™ Step 5 · Genie X Converter™ Step 6).

### ⬜ NEXT — Step 5 · Cash Flow Engine™ (Pillar 2 · Money Magnet™)
- "Turn event ticket registrations into instant cashflow and collect revenue before the event starts — self-liquidating the ad spend." Its own builder prompt + guide.
- Includes the **thank-you / registration-page video** asset (flagged above). Aligns with the Cash Flow Engine™ Calculator (https://icoachlaunch.github.io/cash-flow-calculator/) and Cash Flow Max™ (the VIP/bumps it monetizes).
- Then Steps 6–9 onward.

### ✅ DONE (2026-07-14) — `bits/` — THE SPEAKER BIT LIBRARY (story layer for the Sniper Presentation™)
- **What it is:** a growing, reusable library of **speaker bits** — timeless stories slotted into a presentation to do ONE job (engagement · hype · empowering-belief · belief-break · objection-break · close-prime) and raise emotion → more sales. Matthew's concept; feeds Step 4.
- **Structure:** `bits/README.md` (how it works, the 6-part anatomy, the placement×job taxonomy, the attribution/golden-rule, the SCORE-slot map), `bits/_bit-template.md` (generic skeleton), `bits/pre-pitch/` (front half) + `bits/re-pitch/` (close). **No `pitch/` folder — the pitch is the Sniper Close sequence.**
- **6-part anatomy:** HOOK (engagement setup) · STORY (metaphor + call-&-response) · TURN (bridge to them) · TARGET (belief/objection broken) · SOUNDBYTE · TRANSITION.
- **10 seed bits (all verified 6/6 sections + tags):** pre-pitch = train-the-room (engagement), take-the-shot (fail-to-win), the-bamboo (invisible-growth), the-crossroads (indecision); re-pitch = get-in-the-room (DIY→need a guide), release-the-grip (money/letting-go, Pirsig monkey trap), down-before-up (the dip is growth; seed+eagle), the-crab-bucket (external/social objection), the-real-price (cost of inaction), the-time-bank (no-time objection, 86,400).
- **Sourcing = original + public-domain** (Matthew's call): classics retold in our words + credited; personal stories must be the client's OWN. No fabricated stats (avoided the disputed learning-pyramid numbers). Verified facts: Jordan 1990 Nike "Failure" quote; Pirsig "South Indian monkey trap" (1974); 86,400 time-bank parable.
- **Grows over time:** drop new `.md` files into `pre-pitch/` or `re-pitch/` from the template. NOT surfaced on the dashboard yet (resource folder for now, per Matthew).
- **✅ SOURCE EXAMPLES REPURPOSED (2026-07-14, same session):** Matthew's supplied example bits are now IN the project at `bits/_source-examples/` — raw originals preserved in `_originals/` (8 docx), and **7 repurposed into the 6-part anatomy** (pre-pitch: transformational-learning, fear-of-failure; re-pitch: giraffes-vs-turtles, information-alone, law-of-advancement, monkey-paw [both Joseph Aaron + Prince EA versions], two-biggest-expenses), each crediting the original speaker (Myron Golden, TD Jakes, Steve Harvey, Prince EA/Joseph Aaron, Austin Ford). `_source-examples/INDEX.md` maps every example to its repurposed file + raw docx + the source video path (videos ~2.9GB, LEFT in Matthew's Downloads folders — too big to copy). **3 bits are video-only and still need transcripts to repurpose:** 4 Levels of Value (Myron), Making Decisions As Entrepreneurs (Myron), The Freedom Multiplier Exercise (Frank Kern) — Matthew drops a transcript into `_originals/` and I'll structure them. Each clean-room original in `pre-pitch/`/`re-pitch/` is cross-referenced to its source-example twin (e.g. release-the-grip ↔ monkey-paw).

---

## GitHub
- ✅ **PUBLISHED (2026-07-14):** repo **`icoachlaunch/coach-launch-system`** (remote origin, branch `main`), **GitHub Pages LIVE** (root). Committed + pushed all work via the Desktop Commander MCP (git on the real machine) and confirmed HTTP 200 on the live pages.
- **Live dashboard (Training Portal):** https://icoachlaunch.github.io/coach-launch-system/dashboard.html
- Other live pages: `/index.html` (internal hub), `/slide-viewer.html`, `/guides/<name>-guide.html`. Raw prompts under `/prompts/`.
- **To publish future changes:** `git add -A; git commit -m "…"; git push origin main` (Pages redeploys automatically). Can be run via Desktop Commander from the real machine.
- Tidied out the `.bak` backups + temp helper scripts in a cleanup commit. (Older note: steps also in `SETUP-GITHUB.md`.)
- ⚠️ **Added `.nojekyll` at repo root (required):** GitHub Pages runs Jekyll by default, which (a) converts any `.md` with YAML front matter to `.html` (so `skills/presentation-engine/SKILL.md` 404'd as raw) and (b) excludes `_`-prefixed files/folders (the `bits/_*` files). `.nojekyll` makes Pages serve everything raw. Keep it.

## Sniper Presentation™ Slides — Step 4 · Part 2 of The Sniper Close™ (CLIENT KIT = 3 DELIVERABLES)
> **⚠️ RENAMED + PACKAGED (Matthew, 2026-07-14):** "The Presentation Engine™" is **RETIRED** — the tool is now **Sniper Presentation™ Slides**, framed as **Step 4 · Part 2** of The Sniper Close™ (Part 1 = the Sniper Presentation™ / the live pitch + script from the Sniper Close™ Builder; Part 2 = this, which turns that script into the slide deck). So **Step 4 is now a 2-part sequence.** ™ sits on "Sniper Presentation™"; "Slides" is a plain descriptor. All files packaged into a **self-contained kit folder `/sniper-presentation-slides/`**: `guide.html`, `SKILL.md`, `slide-viewer.html`, `images/` (deck-sample sources), `README.md`. The standalone `prompts/presentation-engine-prompt.md` was **retired** (SKILL.md is the canonical brain). Dashboard `#prompt-data` key renamed → `sniper-presentation-slides-skill`. `apply_formula_map` note: guide keeps its Step-4 map (already stamped); new filename stem is `guide` so pass `--step 4` if re-running. **TODO (not yet done):** update the Sniper Close™ (Part 1) guide/prompt to reference this as Part 2, and consider showing Step 4 as 2 parts on the dashboard/map.
>
> Paths below now live under `/sniper-presentation-slides/`.

Per Matthew, this section ships **three** client deliverables on the dashboard, all live:
1. **Companion Guide** — `guides/presentation-engine-guide.html`.
2. **Slide Builder Skill** — `skills/presentation-engine/SKILL.md` (skill-format: YAML frontmatter `name: presentation-engine` + description + execution instructions, then the full A–J builder body; modeled on the Fletcher `slide-builder-skill.md` Matthew supplied, rebuilt in canon). Installable at `.claude/skills/presentation-engine/SKILL.md` or paste-able. Embedded in dashboard `#prompt-data` under key `presentation-engine-skill` for offline copy/download (Copy skill / Download .md → `presentation-engine-SKILL.md`). The house prompt `prompts/presentation-engine-prompt.md` remains as the source (same content).
3. **Slide Viewer Template** — `slide-viewer.html` (the single-file offline player; ships with the 11-slide sample deck; Open/Download).
- Dashboard Presentation Engine™ card now shows these 3 action rows; index.html mirrors them. Verified: 15 `#prompt-data` keys, all data-copy/data-dl map, all guide links resolve, SKILL.md serves raw 200.

<!-- end of progress log -->
