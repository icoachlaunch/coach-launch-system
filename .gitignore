# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# local server / build artifacts
__pycache__/
node_modules/

# sandbox artifacts (safe to delete)
*-synctest.txt
hdr

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Local review folder - contains Matthew personal deck content (testimonials, prices).
# The repo publishes to GitHub Pages, so this must NEVER be committed.
Review/
