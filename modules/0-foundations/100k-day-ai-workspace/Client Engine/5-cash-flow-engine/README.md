# Step 5 · Cash Flow Engine™
**Money Magnet™ · Pillar 2** — the self-liquidating event funnel

Your funnel is built in **GHL AI Studio** with **the Cash Flow Engine™ Funnel Builder** (`skills/cash-flow-funnel-builder/`). It turns your Event Magnet™, Money Model, Brand Profile and Brand Kit into one build prompt per page, in funnel order.

Save those build prompts here — make a folder per phase as you build it (`phase-1-launch/`, `phase-2-execute/`, `phase-3-growth/`, `phase-4-mastery/`), one `.md` per page. The funnel itself lives in GHL.

**Needs first:** Step 3 (Event Magnet™, Money Model, Cash Flow Max™) and Step 4.

Tell Claude *"build my Phase [N] funnel pages"* and it opens the skill. Save each finished build prompt in its phase folder here. Never a -v2.
